# RABBIT — main conversation / local Codex handoff

Status: OWNER_REQUESTED_EXECUTION_POLICY_AND_NEXT_LOCAL_ASSIGNMENT; not a scientific PASS.
Date: 2026-09-06. Repository: cosmosapjw-quantum/RABBIT_v1.0.5_PRODUCTION.

## 1. Current owner decision

The main conversation performs all feasible mathematics/physics research, source review, coding, small computations, evidence intake and scope/claim adjudication. A separate WORK_THREAD is no longer a required intermediary. Delegate to local Codex only work requiring the actual local repository, native toolchain, installed mathematical packages, heavier execution or local-only resources. Do not delegate work merely because it is coding.

During a local assignment, local Codex is the only writer to its isolated candidate worktree. The main conversation does not concurrently mutate that candidate. Return directly to the main conversation; do not require a work-thread review or exact-SHA publication ticket before authorized isolated-branch publication.

Within the same objective, scope, frozen gates and scientific invariants, Codex must diagnose and repair supported implementation defects, then rerun affected tests until the acceptance criteria are met or a genuine unresolved boundary is reached. One repair cycle is NOT one edit or one edit/test attempt. Preserve every original failure. Do not stop solely because a first implementation or numerical test failed when a same-scope correction is evidence-supported.

This supersedes earlier coordination-only, read-only-only and stop-on-first-test-failure wording for the next bounded upstream repair assignment. It does not authorize threshold widening, reference replacement, altered equations, new retained/holdout work, or silent scientific claim promotion. Experimental one-shot trials and explicit model-call budgets remain distinct from productive Host/Codex editing; do not reset or bypass their durable counters.

## 2. Global harness readback and precedence

Observed global harness: cosmosapjw-quantum/codex_global_harness.
Observed main commit: bbb040d0e6ba8c8c1e8d49cae7d39da6d7ef9d82.
Observed tree: f87c513c2fd766599a09fe329f806ac4782b23f5.
Merged PR #34 covers fleet invocation, retained complete-file transport, logical interpreter lanes and runtime artifact closeout. Repository implementation and reported tests are not proof of this host's installed configuration or RABBIT scientific admission.
Read the applicable local harness and repository policies. Relevant global references are AGENTS.md, docs/UNIVERSAL_PROGRESS_IDENTITY_POLICY.md and docs/RUNTIME_ISSUE_CLOSEOUT.md at the observed commit. Older prose contains bounded retry/review language; the current owner's productive-correction decision above governs this assignment without modifying frozen experimental call budgets. Do not impose an obsolete mandatory universal audit compiler as a new predecessor; PR #34 records PR #21 as superseded rather than merged.
Preserve typed identity: immutable input/source bytes, content/packaging identity, numerical parity and scientific equivalence are separate relations. Mutable branch drift alone is not an immutable-source failure. A policy readback is not installation/update evidence. Do not change global hooks, services, defaults or credentials in this task.

## 3. Documentation anchor is NOT the scientific execution base

This handoff's documentation branch starts from published RABBIT main commit 93303c475a4746107d462ce259c401d9312ceb3b, tree 6a34431e142cf4f2541b2f96f4c568dd184e43a7. That is a transport/documentation anchor, NOT the next science source checkout.

Historical tested scientific source:
- Commit: 2a40780c48eca9584676b72cf785cfada7da563e.
- Tree: 78a4bd6f2e68dbe3bb5450c94d5e17138e777926.
Historical evidence candidate:
- Commit: 603ee2d5f358488cee84161eaf941b05f1c43edc.
- Tree: f43e87ff89fb13c086237de96893ce529a6776a3.
Inputs: P1_ORDER8_EQUILIBRIUM_CAPTURE_RETURN.zip, TARGETED_REFERENCE_FINDINGS.json, EQUILIBRIUM_FAILURE_CLASSIFICATION.json, and the existing source/capture manifests.
Find these in the existing local RABBIT repository or prior rabbit-executor return directory. Reuse the verified bundle/capture; do not repeat full acquisition or demand that a remote mutable branch still points at a historical commit. If a same-scope successor already exists, inspect and reuse it, recording the exact selected commit/tree and semantic delta instead of duplicating implementation.
Keep the historical STOP_ORDER8_COMPONENT_PARITY_CAP unchanged. It is evidence, not a ban on useful same-scope correction or honest checkpoint publication. Reported baseline: thermal_split 330 metrics PASS; equilibrium 158 component metrics exceed the existing cap. The captured reductions do not identify final summation as the sole cause; shared nodal inputs lead to differing coefficients/interpolated logits/affinities. Do not treat a case named equilibrium as exact detailed balance.

## 4. Next bounded objective

Work unit: P1_SHARED_INPUT_UPSTREAM_DISCRIMINATOR_AND_BOUNDED_REPAIR_V2.
Use the shared-input discriminator to identify the earliest supported implementation defect in coefficient construction, interpolation or affinity formation. Repair that defect only if the same source-defined finite operator, input representation, conventions, branch semantics and frozen acceptance gates are preserved. Return the diagnostic and any actual repair/test evidence in the same handoff.

Before editing, read the actual governing P1 contract and source maps. Record the exact affected files/functions and allowed mutation-path union in the existing assignment machinery; do not create another readiness framework. Candidate implementation scope is Rust upstream coefficient/interpolation/affinity evaluation and strictly necessary dependent tangent/registration changes. A semantic change to the finite operator is not automatically authorized.

A. Read the actual Python/Rust finite transform definitions. Distinguish quadrature projection, solve and recurrence; do not assume a dot product model where the source uses another transform. Preserve the frozen Python authority as a reference, not a target to edit into agreement.
B. On stored binary64 operands, evaluate exact captured-word dot products and the symmetric decomposition: delta(u3) = mean(b3)^T delta(c) + delta(b3)^T mean(c) + delta(interpolation evaluation error). Decode exact float words, not shortened decimal strings. Attribution is not an independent accuracy certificate.
C. Evaluate the source-defined common finite transform at high precision using the same frozen nodal/grid inputs. Keep captured-word exact reduction distinct from mathematical finite-transform accuracy. Precision-doubling agreement is a numerical cross-check unless a rigorous enclosure is actually obtained.
D. Separate logit-input discrepancy, four-term affinity-formation error and subsequent Pauli evaluation. Compare captured staged affinity, exact rational four-logit sum and once-rounded binary64 shadow affinity. Do not change logits, zero high-order coefficients or project to equilibrium. A Phi-only shadow is a diagnostic, not a complete new thermal JVP.
E. If an implementation defect is supported, write the smallest falsifying regression before the repair, implement it, and continue evidence-driven edit/test/diagnose cycles within this scope. Reuse the existing build/test/oracle infrastructure. Avoid simultaneous broad refactoring.
F. Run the narrow repaired checks, affected predecessor regressions and the existing order-8 component comparison when the changed path is ready. Preserve signed components, raw arrays, real mask/sample identity, reconstruction and independently formed energy ledgers. Extend to the remaining required P1 checks only when their prerequisites are met; do not call a partial correction full admission.

Do not widen the 1e-7 component parity cap, alter other frozen caps, remove failing cases, substitute total-only comparison, redefine the reference, use observed discrepancy as its own accuracy bound, or infer validity from file hashes. No retained/holdout, packed-RHS column, solver, trajectory, endpoint, performance, QKE or public-production work is authorized. If only a reference/metrology or semantic-operator amendment could proceed, stop that scientific mutation, preserve the analysis, publish an accurately labelled return, and ask the main conversation for that specific decision.

## 5. Actual local execution, not environment rediscovery

Use the existing installed mathematical packages and working native capsule. Record logical interpreter path, version and exact invoked argv; do not replace a venv entry point with its resolved base executable. Existing Rust 1.94.1 and locked offline dependencies remain the production target unless the governing contract explicitly differs. Reuse saved lane receipts; probe only the capability needed for this work. A failed optional engine does not block unrelated executable work or count as scientific refutation.
Do not install models, change global runtime bindings or repair unrelated hooks to complete this unit. Actual independent reviews/CAS axes must be honestly attributed. Local model/artifact-channel limits are not permission for unlimited inference, and failed review routing is not evidence that an independent review passed.
Keep one logical task directory across interruptions. Preserve unknown/pending subprocess outcomes and reconcile them before rerunning. Narrow compile/fmt/lint checks should precede expensive evaluations where feasible. Zero executed tests are NOT a passing test result.

## 6. Git is the normal handoff and return channel

The owner authorizes non-force pushes of in-scope source, handoff, summary, metrics and evidence to a new isolated task branch, including honestly labelled partial/failed checkpoints. Do not require another publication ticket. Preserve earlier STOP/PASS receipts verbatim; append new results. Never move main/canonical/historical evidence refs, merge/rebase/force-push, create a release/tag, or widen the task silently.
Create a uniquely named branch such as agent/rabbit-p1-upstream-repair-<UTC>-r1 from the exact selected science candidate, not from this documentation branch. Copy this governing handoff into the new task's existing handoff/context location and bind its actual source commit/blob. Do not duplicate a large archive just to transfer a small result.
At closeout, commit directly readable RETURN_TO_MAIN.md, a compact strict JSON receipt and relevant machine-readable metrics, plus scripts/patches needed to reproduce the result. Sanitize secrets, tokens, credential-bearing URLs and unrelated private information before publication; RABBIT is public. Do not copy private global-harness source or private profiles into this repository.
Preserve raw logs once. For large raw archives, use already-approved durable storage or existing configured LFS; put location/digest/size in Git. Do not invent an upload or change storage policy. If raw bytes cannot be hosted, publish the compact reports and mark RAW_NOT_REMOTE with the exact local retained location; do not send the user back into a mandatory manual ZIP relay.
Push the task branch, then read back remote ref, commit/tree and result file blobs. Distinguish tested scientific tree from evidence-only append. Evidence-only append does not by itself require another full native replay. CI execution, when available and useful, is separate from local validation; report actual run/attempt/job and outcome only.
If a push fails, preserve the local commit/bundle and report PUBLICATION_BLOCKED separately from science status. Retry only when credentials/routing/network state materially changes; do not repeatedly rediscover a failing tool. Do not claim background monitoring or a scheduled follow-up.

## 7. Compact direct return to the main conversation

No work-thread intermediary is required. End the local response with a short summary and these fields:

```text
STATUS / ACTUAL_PROGRESS / VERIFIED / NOT_VERIFIED / BLOCKERS / NEXT
REPOSITORY / BRANCH / BASE_COMMIT / BASE_TREE
TESTED_COMMIT / TESTED_TREE / EVIDENCE_COMMIT / EVIDENCE_TREE
HANDOFF_URL / RETURN_TO_MAIN_URL / RECEIPT_URL / METRICS_URL
RAW_EVIDENCE_LOCATION / RAW_EVIDENCE_SHA256 / RAW_REMOTE_STATUS
COMMANDS_AND_COUNTS / ORIGINAL_FAILURES / REMAINING_GATES
HARNESS_COMMIT_READ / LOGICAL_RUNTIME / INDEPENDENCE_STATUS
CHANGED_PATHS / ADDED_DELETED_NET_LINES / TOKEN_USE_OR_UNAVAILABLE
REMOTE_READBACK / CI_RUNS_IF_ANY / CLAIM_CEILING
```

Use immutable GitHub commit-based file URLs for the main return. The main conversation reads those links directly. A result document cannot contain its own final commit hash before it is committed; report the resulting evidence commit in the terminal output or a subsequent metadata-only record, not through a self-referential hash fiction. No new science test, CI result, installed-harness claim or P1 admission is created by this handoff.

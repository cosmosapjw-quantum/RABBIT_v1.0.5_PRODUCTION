# The environment that ACTUALLY produced this repository's recorded results.
#
# Captured from venv/ (CPython 3.12.3) on 2026-08-06 by BD623. This is a
# measurement, not a preference: pyproject.toml declares lower bounds only
# (numpy>=1.24, scipy>=1.10, ...) with no upper bound and, before this, no lock
# at all, so nothing recorded which versions the numbers were produced with.
#
# WHY IT IS PINNED TO WHAT WAS MEASURED RATHER THAN TO THE NEWEST RESOLUTION.
# An unconstrained `uv lock` on this pyproject resolves scipy 1.18.0 and numpy
# 2.5.1 for python>=3.12. Nothing in this repository has ever run those. On this
# programme specifically that is not a hypothetical risk: SciPy's finite
# difference Jacobian step behaviour (`num_jac`) is what the D-069/D-071
# trajectory measurements turned on, and a minor-version bump is exactly the
# kind of change that moves it. A lock that pins an environment nobody ran would
# make the repository look reproducible while reproducing nothing.
#
# Used as the constraint set for requirements.lock, which carries hashes:
#   uv pip compile pyproject.toml --python-version 3.12 --generate-hashes \
#     --constraint requirements.measured.txt -o requirements.lock
#
# Moving these versions is a deliberate act that needs its own measurement, not
# a side effect of a fresh resolve.
absl-py==2.4.0
blackjax==1.5
contourpy==1.3.3
cycler==0.12.1
diffrax==0.7.2
equinox==0.13.7
execnet==2.1.2
fonttools==4.62.1
iniconfig==2.3.0
jax==0.10.0
jaxlib==0.10.0
jaxtyping==0.3.9
kiwisolver==1.5.0
lineax==0.1.1
matplotlib==3.10.8
maturin==1.13.1
ml_dtypes==0.5.4
mpmath==1.3.0
numpy==2.4.4
opt_einsum==3.4.0
optax==0.2.8
optimistix==0.1.0
packaging==26.0
pillow==12.2.0
pluggy==1.6.0
Pygments==2.20.0
pyparsing==3.3.2
pytest==9.0.3
pytest-xdist==3.8.0
python-dateutil==2.9.0.post0
scipy==1.17.1
setuptools==82.0.1
six==1.17.0
typing_extensions==4.15.0
wadler_lindig==0.1.7

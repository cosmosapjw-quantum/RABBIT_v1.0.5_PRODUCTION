# Project State

Date: 2026-07-24

## Current status

The active remediation branch is `f10-independent-validation-b3v2`, based on the
curated fresh-history public baseline `main@ca3a0138d496732edfc13559fd8f7ceec7ef4d6e`.
The pre-publication Git database and local evidence were preserved rather than
deleted under ignored `.publish-local/20260719T052443Z/`; they are not part of
the publishable tree. `out_of_scope/`, raw harness runs, generated outputs,
compiler/profiler products, audit drops, caches, archives, and local reference
PDFs are likewise absent from every public commit. The retained `venv/` is
local and ignored; it was pruned to the installed project closure while its
byte-preserving pre-prune copy was retained. It is not a complete `.[dev]`
environment because declared optional `dynesty` and build requirement `wheel`
were already absent. This maintenance overlay changes no scientific claim or
gate below.

The augmented Type-I PSTF no-QKE programme remains RED overall. The sole
future-order authority is now the bottom-up plan in
`docs/TYPEI_AUGMENTED_NOQKE_UNIFIED_FUTURE_PLAN.md`.

Rust AOT remains the active implementation and repeated-run target. It is the
private number-of-record only for the exact F-06 minimal matched profile;
F-07 through F-08N are conditional correction/network layers with narrower
evidence ceilings. SciPy remains the general corrected research-runtime
comparison authority because F-08D strict standalone precision remains
BLOCKED and independently validated full spectral transport authority is
absent. F-10C1 selects a fixed positive exponential N48 representation within
a declared five-profile, two-auxiliary-rule `2.1%` envelope. F-10C2 now
executes and internally validates all nine frozen classical diagonal collision
rows and materially moves the whole-endpoint wall. The crate-private dual-
solver endpoint and `N_eff` readouts remain internal regression evidence. Two
private structurally independent full-spectral static formulations have run
and failed their frozen M1 discriminators before trajectory authority. A third
three-node maximum-relative-entropy route was rejected at blind design review
before implementation because its derivation and executable selector are not
one coherent continuous/covariant method. No independent FLRW endpoint has
run.
JAX remains frozen as a non-dispatchable
bounded component parity, AD, Jacobian, and numerical-method oracle. QKE and
public production are FORBIDDEN. By the 2026-07-16 owner decision, the current
programme stops after F-10 closes a collision-coupled isotropic neutrino
Boltzmann FLRW endpoint. F-11 Bianchi/Type-I is owner-paused and must not be
started without a new explicit instruction.

## 2026-07-28 D-052--D-056 gate-matrix completion overlay

- Under the D-052 omnibus grant (row-9 closure, covariance-metrology,
  trajectory/endpoint, sequenced), all three stages closed in one session:
  D-053 accepted the final static catalogue `760a7c04` (all pair
  conversions orientation-closed); D-054's first metrology contract FAILED
  on two adjudicated design defects (preserved) and the corrected D-055 r2
  PASSED, flipping `G-F10-COVARIANCE-METROLOGY`; D-056's structurally
  independent SciPy-BDF trajectory reached the 0.005 MeV cold endpoint
  with `N_eff = 3.034054` (delta `+7.4e-4` vs the Rust N48 anchor, band
  `3e-3`), `t_end` delta `-1.47 s`, clean blockwise hierarchy and first
  law, flipping `G-F10-INDEPENDENT-FLRW`.
- **All eight F-10 gates are PASS.** The independent-validation gate
  matrix is complete on catalogue bytes `760a7c04`.
- Boundaries unchanged: matched-resolution N48-class endpoint agreement on
  the frozen 10 MeV -> 5 keV cell only; no continuum claim. Unblinding,
  public/production claims, W7/B3, T01--T12, GL64/Radau, Rust/JAX forward
  work, F-11/Bianchi, and QKE remain closed and are separate owner
  decisions under `G-F10-SCOPE`.

## 2026-07-27 D-049/D-050/D-051 lint-fix and OWNER-C closure overlay

- D-049: the sole regression-bundle failure (test-only clippy lint on the
  ten-argument `gk_recurse` recursion) was fixed by a single `#[allow]`
  attribute (commit `1697e76`, zero codegen change); the full bundle then
  passed (fmt/check/clippy clean, 238/0 release tests, 54-page report
  clean) and **`G-F10C1-REGRESSION` flipped fail -> pass**.
- D-050/D-051: under OWNER-C with a prospectively frozen contract, the
  live comparator now carries the D-046/D-048-validated row-6 two-member
  orientation closure (`_independent_noqke.py` sha256 `29a652fd` at
  commit `d22d001`; catalogue 24 -> 25; both members at `K_t`/`16.0`).
  All frozen checks pass: 3/3 focused tests including the D-028
  executing-gate assertion; GL48 `mu_tau_residual =
  2.6019033208963758e-11` (18x under the `1e-10` cap); conservation,
  first law, CP, entropy, and the equilibrium null preserved at their
  recorded classes. Terminal: `STATIC_GATES_PASS_ON_CLOSED_SOURCE`.
- Unchanged: the historical D-028 FAIL for bytes `535370c1` stands;
  `G-F10-COVARIANCE-METROLOGY` remains FAIL (identity + `B_native`
  program unbuilt); `G-F10-INDEPENDENT-FLRW` remains FAIL. Row 9 remains
  single-membered (open question). Trajectory/endpoint eligibility is
  newly live as a question for a separate explicit owner decision.

## 2026-07-27 D-047/D-048 OWNER-B target replay overlay

- D-047 recorded the OWNER-B grant and prospectively froze the read-only
  target replay (contract + script at commit `9c99ecf`): external mirror of
  the row-6 event loop, caller-built reverse ordered member, D-046 derived
  one-half closure, gated checks T-A..T-H, explicit D-028 non-reopen
  scoping. `_independent_noqke.py` imported unmodified.
- D-048 closed the replay as an adjudicated PASS on all nine gated checks
  (single frozen execution, wall 18.88 s). T-A reproduced the recorded
  D-028 residual digit-for-digit (`4.666064056497196e-10`); the live-code
  identity `modal_M-(f) == P.modal_M+(Pf)` is bitwise zero on both frozen
  states; the closed-total native mu-tau residual is
  `3.8807636157303184e-11 <= 1e-10`; deleting row 6 alone gives
  `4.1812242095923674e-11` (internal localization); the single-member
  artifact is order one; conservation holds at `2e-16`.
- Scientific status: the D-028 residual mechanism is internally established
  as the row-6 single-ordered-member orientation artifact under `1/y^2`
  amplification, removed by the derived-quotient closure.
  `D046-r6-orbit-chart-rabbit-applicability = VALIDATED` (bounded to the
  two frozen states; no metrology bound, general-state proof, or
  trajectory/endpoint claim). The D-028 recorded FAIL for its exact bytes
  stands unreopened; `G-F10-COVARIANCE-METROLOGY` does not move.
- Terminal line `REQUEST_OWNER_C_CONSIDERATION` is a request only;
  `OWNER-C=REQUIRED_NOT_GRANTED` for any `_independent_noqke.py`
  modification. Recorded limitations: same-model reviews, single
  deterministic execution, T-B bitwise-false at `1.01e-14`, undecomposed
  `4.18e-11` sub-cap floor, W5 drift provenance, adapted identity state.

## 2026-07-27 D-045/D-046 owner-grant and OWNER-A discriminator overlay

- D-045 recorded the 2026-07-27 owner grants: handoff durability, one
  validation-only `G-F10C1-REGRESSION` bundle re-run, and OWNER-A for one
  prospectively sealed, repository-free, nonphysical `C-R6-ORBIT-CHART`
  discriminator with local sealed execution. OWNER-B (target replay) and
  OWNER-C (`_independent_noqke.py` modification) were defined and remain
  ungranted.
- The regression bundle ran in full on commit `b2542fc`: fmt, check, the
  238/0 release test suite, and the 54-page report build all pass; clippy
  fails with `too_many_arguments` on `gk_recurse`
  (`src/isotropic_boltzmann.rs:2513`, lib-test target), a successor-branch
  lint regression absent from historical closeouts. `G-F10C1-REGRESSION`
  therefore stays FAIL on fresh current-tree evidence; the fix is a separate
  owner decision (validation-only stage edited no source).
- D-046 closed the OWNER-A discriminator as an adjudicated PASS. The sealed
  lab (`BD622_OWNERA_R6_ORBIT_CHART_LAB/`, seal `4c05ce2`, run
  `run-20260727-f10-ownera-r6-orbit-chart-lab`) proves on manufactured
  nonphysical objects that the two-ordered-member orientation orbit with the
  derived Reynolds one-half quotient restores mu/tau modal equivariance that
  a single ordered member exactly lacks, with retained antisymmetric
  response, exact conservation/entropy certificates, two-contribution JVP,
  10/10 preregistered mutant kills, and three byte-identical isolated
  replays.
- Claim statuses: `C-R6-ORBIT-CHART-MICROCASE=VALIDATED` (manufactured
  nonphysical microcase only), lab code `IMPLEMENTED`,
  `C-R6-ORBIT-CHART-RABBIT-APPLICABILITY=PROPOSED`. Recorded limitations:
  same-model blind reviews, pre-seal static fix loop, probabilistic MUT-5
  kill, single-host determinism, version-string pins.
- Terminal line `REQUEST_OWNER_B_AUTHORIZATION_FOR_TARGET_REPLAY_REVIEW` is
  a request only. `OWNER-B=REQUIRED_NOT_GRANTED`; every scientific gate is
  unchanged; all hard stops remain in force.

## 2026-07-24 D-044 open-breakthrough intake closeout overlay

- The external response `F10_OPEN_BREAKTHROUGH_LAB_ab95ff52.zip` to the D-043
  corrected request was intook read-only in registered run
  `run-20260724-f10-open-breakthrough-intake` (closeout run
  `run-20260724-f10-open-breakthrough-intake-closeout`, context `815eb4b9`).
- ZIP transport and the 15-file manifest verify, but package admission is
  `PROVENANCE_FAIL` at `XG-00-PROVENANCE` with `PR_DAG=UNREACHABLE` and
  `DO_NOT_REOPEN` for these exact bytes. The package has no prospective
  `EXPERIMENT_CONTRACT.json`, pre-output chronology, single no-argument
  runner, one-finalist execution, raw stdout/stderr, complete manifest,
  pinned network-off isolation, semantic mutants, clean replays, or
  accounting/terminal route, and it imports the live RABBIT comparator while
  emitting physical collision actions.
- Six pre-adjudication envelopes contain 40/40 exact-unique findings; the
  final adjudication `7beccc88` resolves the sole substantive conflict by
  separating flavour permutation from incoming/outgoing slot reversal. Two
  invalid Stops (provenance direct-file role-hash, cross-admit
  aggregate-status) were blocked before corrected envelopes were accepted.
- Row-6 localization is retained as bounded external evidence only.
  Label-only OCAR is rejected: with `P` acting on mu/tau labels the correct
  slot identity is `D(Pf;z)=-D(f;Rz)`, so flavour canonicalization alone
  cannot produce a reverse orientation chart. The narrower, explicitly
  orientation-closed `C-R6-ORBIT-CHART` remains `PROPOSED` with
  `OWNER-A=REQUIRED_NOT_GRANTED`.
- No collision runner, target replay, code/spec/gate edit, W7/B3, T01--T12,
  Rust/JAX, Radau, trajectory, endpoint, or unblinding ran. Every scientific
  gate state is unchanged.

## 2026-07-24 D-043 provenance-contract correction overlay

- The first external D-042 attempt is preserved as `PROVENANCE_FAIL` at
  `XG-00`. It correctly refused to reconstruct a requested frozen-decision
  snapshot or accept missing historical context-pack and `/tmp` artifacts.
- A full local search of branches, tags, remote refs, reflogs, reachable and
  dangling objects found `4f4da8f0` only as unreachable blob `5d583891` in
  orphan trees; it was never a distributable commit. `e3ea99a9` is absent from
  the Git object corpus. Neither byte is promoted or reconstructed.
- The owner authorized an in-place prompt correction. The external input
  contract now uses immutable code snapshot `bede5a9`, published evidence
  floor `204d3fa`, and the future commit containing the corrected prompt.
  Missing old contextual evidence prevents reuse of the affected old claim but
  does not block an independently derived, prospectively sealed candidate.
- `XG-00` remains strict for every binding published input and all new
  contract/source/test/raw-output/manifest bytes. The correction opens only the
  already bounded external 6--10-family screen and at most one repository-free,
  nonphysical microprototype. `G-EXT-FEASIBILITY` and every scientific gate
  remain FAIL; the repository PR queue remains empty.

## 2026-07-22 B3-v2 successor entry-gate overlay

- The rejected branch is preserved. The successor branch starts from
  `main@ca3a013`; `.claude/settings.json` was not carried forward. The
  pre-existing AD/Bianchi design is retained only as `PROPOSED/PARKED`, with
  implementation `FORBIDDEN` while F-11 is owner-paused.
- W3 is limited to `DERIVED` coefficient-ratio/kernel-assignment and MB-reduction
  evidence. W5 is `VALIDATED` localization evidence, not an exclusive root-cause
  proof. The corrected W6 B3-v2/W7 contract is `PROPOSED / DESIGN ONLY`.
- The successor method evolves six explicit species, separates finite-`m_e`
  electron shells from massless self kinematics, uses reaction-specific weak
  invariants and Cholesky `M^-1q`, and requires W7 absolute-normalization plus
  method-specific MPFR metrology before any B3 collision output.
- Preserved run `run-20260722-f10-b3v2-blocker-remediation` contains two failed
  canaries. R2 observed `SubagentStart`, but its preflight incorrectly searched a
  payload that cannot contain the spawn prompt; after the controlled negative stop,
  `stop_hook_active=true` bypassed validation of the incomplete retry artifact.
- Reloaded-session R3 proved that `SubagentStart` injection, retry-safe
  `SubagentStop` blocking, final envelope validation, and result-only writes work.
  It also proved that VS Code collaboration `spawn_agent` bypasses project
  `PreToolUse`: both allow-rewrite attestation and a reserved-field block probe
  failed at launch. Those raw failures are preserved and D-031 is superseded.
- The executable workaround is **VALIDATED** for the registered R4 chain: main owns registered
  launch admission plus pre/post hashing; `SubagentStart` injects context/identity;
  each result carries a prompt/Start `spawn_contract` and assignment-file SHA-256;
  `SubagentStop` validates that contract and the final envelope on every attempt.
  Four isolated fixtures pass. R4 result `5a979857` records Start preflight PASS,
  the blocked invalid first stop, and accepted corrected result; identical pre/post
  non-run diff `828cd996` and status `7ad89840` prove result-only writes.
- R4 validated only the supported parent/Start/Stop lifecycle. The later D-034
  Sage/Singular axis created undeclared repository-root `.singularhistory`
  (`5abbc842`) while the tracked non-run diff remained `eb4e436c`. Effective
  `G-HARNESS-INTEGRITY` is therefore **FAIL** until D-035 confines that tool to an
  assignment-specific temporary cwd and a fresh run preserves every non-run hash.
  VS Code collaboration `PreToolUse` remains unavailable and is not an authority
  boundary.
- The registered review `run-20260722-f10-b3v2-w6w7-blind-review` is complete.
  B3 design `fc283d4f` and W7 metrology `8d490da0` failed; Wolfram `4d81d99c`,
  Sage `722dfa8a`, Lean `b5c61f02`, and SymPy `21abb162` are overall
  inconclusive under the four-axis contract. The normalized merge is
  `1c28dcfb`; adjudication `e5b21e26` is **FAIL**.
- The bounded exact reaction-nullspace subclaim passes all four engines, and
  three engines agree on the direct row-ii trace and entropy affinity. Those
  algebraic results do not repair missing all-channel normalization, exact
  reaction/sign/multiplicity records, finite-`m_e` implementation roots and
  Jacobians, deterministic basis/QR and ladder definitions, or MPFR/`B_native`
  numeric bytes.
- Dedicated CAS/adjudicator collaboration types could not start because their
  forced model is unsupported for this ChatGPT Codex account. Registered
  default-agent fallbacks used the actual Wolfram, Sage/Singular, Lean, and
  SymPy tools plus the exact role files. This retains evidence but not a claim
  of canonical dedicated-agent completion.
- W7 numerical execution and static implementation still require explicit
  owner authority, but owner authority is not sufficient now. Remediation and
  a fresh blind PASS on exact bytes must occur first. No forbidden execution or
  production edit occurred.
- The owner approved D-033 remediation and a fresh blind review only. D-034 now
  freezes candidate W6 `e0103e29`, unexecuted W7 contract source `5e8842d9`, and
  exact vectors `5a16c903`. A source-only structural check passes the 24+15 graph
  and source/vector identity. This is `SPECIFIED_NOT_EXECUTED`, not validation.
- The fresh D-034 run `run-20260722-f10-b3v2-d034-blind-review` is terminal
  **FAIL**. CAS contract `04e0960b`, B3 `39e9394a`, W7 `98a73fce`, Wolfram
  `91c45257`, Sage `d8e4b2c8`, Lean `f19f64a9`, SymPy `c9f65215`, merged
  `a172b68f`, and adjudication `95870216` retain 31 submitted/31 unique findings.
  The exact reaction-nullspace passes all four axes; row-ii and entropy retain
  bounded three-axis passes with a Lean tool ceiling; physical-interior finite-
  `m_e` shell formulas are derived. Those subclaims do not repair global/tagged
  orientation normalization, the Tier-0 `G_F` drift, finite-mass source branches,
  arbitrary-`y` basis/ladder definitions, or A0--A4/`B_native` completeness.
- On 2026-07-23 the owner approved D-035 only: correct those exact bytes, isolate
  Sage/Singular outside the repository, and perform one fresh registered blind
  review. The approval does not authorize W7 numerical output, B3 collision or
  static implementation, T01--T12, Rust edits, Radau, trajectory, endpoint,
  unblinding, F-11, QKE, or public runtime.
- The sealed D-035 bytes remain W6 `9caaa445`, unexecuted contract source
  `60fc9668`, exact vectors `e991d415`, and Sage/Singular role `812bce01`.
  Source-only checks reproduce global-to-tagged 64/32, the three threshold
  outcomes, 24+15 reactions, 20 ladder rows, and source/vector object equality.
  D-037 proves that these consistency checks do not close the executable shell,
  single-basis, directed-tail, event-trace, or A0--A4 contracts.
- The D-035 fresh run stopped before merge/adjudication. B3 `4cb9132c` and W7
  `a998edb4` are admitted FAIL envelopes. Wolfram `4d768662` is preserved but not
  admitted because live Stop rejected the fallback `default` runtime against a
  falsely registered dedicated role. Sage `ff1620c1` is not admitted because its
  uppercase finding verdicts violate the canonical enum; Lean and SymPy were
  interrupted without results. The run establishes no fresh design verdict.
- The owner then authorized D-036 harness repair only. Assignment schema v2 keeps
  `agent_type` and `runtime_agent_type` equal to the actual Start/Stop event and
  independently binds `review_role`, its exact role files, and the canonical
  result template by SHA-256. Static fixtures pass `6/6`, including Stop-side
  rejection of an unregistered logical role. Final live R3 result `1b2f2e23`,
  assignment `7b62b201`, and context `a364e7b4` verify the supported
  `default` runtime with sealed `cas_sage_singular` review role, exact role and
  template hashes, Start preflight PASS, blocked invalid first Stop, accepted
  corrected envelope, `/tmp` Sage/Singular confinement, unchanged `.singularhistory`
  `5abbc842`, and identical pre/post non-run diff/status hashes `a66f19fc`/
  `b1d0bc24`. `G-HARNESS-INTEGRITY` is therefore **PASS** only within this
  supported schema-v2 parent/Start/Stop boundary.
- D-037 used shared CAS contract `d2780e2c` and admitted two schema-v2 results:
  B3 `bead9788` and W7 `f785c2d2`, both **FAIL**. Their 11/11 unique findings
  retain four bounded passes—reaction nullspace, entropy affinity, direct row-ii
  trace, and absolute normalization—but identify seven binding failures in
  per-leg/threshold shell enforcement, coefficient-Horner basis authority,
  directed tail enclosures, canonical event/trace completeness, A0--A4 rigor,
  and the whole B3/W7 designs. D-035's non-PASS rule stopped the run before
  four-axis completion: Wolfram/Sage were interrupted without results,
  Lean/SymPy were registered but never spawned, and no merge/adjudicator ran.
  Parent pre/post diff/status stayed `cea5aa70`/`b1d0bc24`; `.singularhistory`
  stayed `5abbc842`. The effective design verdict is **FAIL at the hard stop**,
  not a completed adjudicator verdict.
- No staged authority opened. W7 execution/static implementation and any B3
  implementation/T01--T12 remain forbidden. A new explicit owner decision is
  required before exact-byte remediation or another review may begin.
- D-038 completed a read-only current/legacy historical audit, two blind audit
  axes, mutual steelman, six-round debate, and final adjudication `c145f298`.
  Its 32/32 exact-unique findings consolidate to seven causal families. Current
  D-035/W6 executable-contract debt is the sealed-candidate root cause;
  history-wide architecture-first is a recurrence amplifier rather than a
  universal root over Fort/D-027/D-028/D-029. Default remains STOP/PRESERVE;
  all four route-readiness verdicts remain **FAIL**.
- D-039 records the owner's final choice of **STOP/PRESERVE**. D-038's optional
  one-final-design-cycle path is frozen. R2 `898fcf02` rejected the first prompt;
  the corrected exact request passed targeted R3 `a424e9c9` with 30 hash checks.
  It may return at most three ranked proposals or a no-go; neither it nor its answer grants repository,
  design-cycle, W7, B3, numerical, implementation, or endpoint authority.
- D-040's read-only current/legacy JAX, primary-literature, Bianchi, and DSMC
  survey is complete under
  `run-20260723-f10-d040-legacy-jax-readonly-survey`. Five registered audit
  axes and adjudication `d7d34e41` normalized 37 raw findings to 34 exact keys.
  The no-stress Type-I shear norm is `DERIVED` monotone; coupled highly
  oscillatory shear remains `SPECULATIVE`. Conventional DSMC is excluded from
  future Bianchi primary, precision, covariance/derivative, and endpoint lanes;
  only a separately authorized bounded qualitative cross-check remains
  `PROPOSED`. Legacy JAX is formula/mechanism evidence only and does not reopen
  JAX development, F-11 implementation, scientific execution, or any D-039
  authority ladder.
- D-041/EXT-01 completed the authorized one-shot, pre-output feasibility
  tournament. Five blind formal/CAS axes, two cross-steelman reviews, and
  adjudication `18d6449d` reduce 33 raw findings to 30 exact keys. The
  same-space PCR-DG wording fails its endpoint theorem, while the distinct
  Petrov, analytic-enriched, and moving-remap families remain unproved rather
  than universally impossible. Zero current candidates close the conjunctive
  gate, so `G-EXT-FEASIBILITY=FAIL`; all three are archived and OWNER-1/PR-01
  are not reached. STOP/PRESERVE and every downstream prohibition remain
  binding.
- D-042 produced the original bounded external executable-breakthrough request
  (`6b5545bd`) and no scientific candidate. Four blind design inputs and two
  hostile reviews forced one-finalist/one-authority-path deflation, prospective
  operation-graph/domain/norm/arithmetic definitions, semantic rather than
  hash/import mutation kills, and separation of mathematical feasibility from
  future repository deletion. Replacement adjudication `9607e9cf` marks the
  original prompt contract internally coherent, but the first external attempt
  returned `PROVENANCE_FAIL` on non-distributable historical prerequisites.
  D-043 supersedes those input/output clauses. The current PR queue is empty,
  `G-EXT-FEASIBILITY=FAIL`, and any returned PASS may only request a new owner
  intake decision.

## 2026-07-18 independent-static fail-closed overlay

- Whole-program FortEPiaNO 1:1 authority is **DEPRECATED**. Only its hash-locked
  classical flavour-diagonal flat-FLRW no-QKE initial-collision slice remains
  contextual; QKE/oscillation/QED architecture is not inherited.
- The direct pointwise independent candidate is **VALIDATED** as an M1 failure
  under D-027. The conservative full-degree Galerkin-Petrov replacement is
  **IMPLEMENTED**, but `GL48_STATIC_R1` is likewise **VALIDATED** as a failure.
- GL48 passes the common-FD null and all recorded S1 conservation, exchange,
  first-law, CP, entropy, family, wall, and memory checks except native mu-tau
  covariance: `4.666064056497196e-10 > 1e-10`.
- D-029 rejects the exact three-node maximum-relative-entropy design before
  implementation. Its fixed-triple theorem is `DERIVED` and its standalone
  local binary64 stencil checks are `VALIDATED`, but the executable's
  quadrature prior, nearest-exterior support selector, and exact-node one-hot
  branch lack a unified continuity/covariance derivation and collision-specific
  binary64 validation over the frozen 24-self/15-electron catalogue.
- D-028 and D-029 forbid GL64, norm/cap changes, symmetry projection, event-orientation
  averaging, reduction retries, Radau, trajectory, endpoint, and RABBIT
  unblinding. `G-F10-INDEPENDENT-FLRW` remains **FAIL**. Another method may be
  considered only after an explicit owner decision and one prospectively
  frozen coherent selector/prior/exact-node/collision contract.

## Active foundation ladder

| Slice | State | Accepted boundary | Next dependency |
|---|---|---|---|
| F-00 | CLOSED_WITH_LIMITS | History/corpus diagnosis and execution-order reset only | no runtime or physics claim |
| F-01 | CLOSED_WITH_LIMITS | VALIDATED crate-private BDF/Rodas5P mechanics only | no BBN/runtime-authority claim |
| F-02 | CLOSED_WITH_LIMITS | VALIDATED declared ideal tree-level FLRW model only | no BBN or standard-`N_eff` claim |
| F-03 | CLOSED_WITH_LIMITS | VALIDATED six-channel Born rates and standalone neutron freeze-out only | no nuclear-abundance claim |
| F-04 | CLOSED_WITH_LIMITS | VALIDATED named 9-species/12-reaction nuclear kernel only | no coupled or final-abundance claim |
| F-05 | CLOSED_WITH_LIMITS | VALIDATED explicit simplified coupled Rust BBN skeleton only | no matched, precision, or authority claim |
| F-06 | CLOSED_WITH_LIMITS | matched minimal private Rust number-of-record plus actual source deflation | no precision/public claim |
| F-07 | CLOSED_WITH_LIMITS | scalar QED EOS; leading endpoint externally checked; complete endpoint internally checked only | material complete-mode cost and no external `dPb` endpoint oracle |
| F-08A | CLOSED_WITH_LIMITS | validated zero-temperature Coulomb plus resummed radiative weak block in the exact matched profile | no precision/full-network/public claim |
| F-08B | CLOSED_WITH_LIMITS | validated additive first-order finite-nucleon-mass Fokker-Planck block with `delta_kappa=0` in the exact matched profile | no stock/precision/C-backend claim |
| F-08C | CLOSED_WITH_LIMITS | validated physical weak magnetism with `delta_kappa=3.70589007463` through the shared F-08B/F-08C finite-mass algebra | no stock/precision/full-network/public claim |
| F-08D | CLOSED_WITH_LIMITS | validated complete four-subterm finite-temperature radiative block and conditional matched endpoint | strict standalone precision parity remains BLOCKED |
| F-08N | CLOSED_WITH_LIMITS | validated Rabbit-selected 31-row AC2024 central-rate network and nested endpoint | not stock PRIMAT, no uncertainty propagation or precision claim |
| F-09 | CLOSED_WITH_LIMITS | validated finite-mass FD/Pauli thermal moment consumed by the crate-private 3T selected-31 endpoint | no spectral/precision/runtime authority |
| F-10 | IN_PROGRESS | F-10A collisionless grid; F-10B0 wall reduction; F-10B1/B2 finite-mass electron spectral action and selected logit endpoint; F-10C1 fixed exponential N48 representation; F-10C2 complete nine-row classical diagonal catalogue, exact BDF cache, and four-topology production aggregation | catalogue and material performance gates pass; structurally independent full-spectral FLRW validation remains the sole scientific closure gate |
| F-11 | OWNER-PAUSED | LRS then non-LRS Type-I | out of the current goal; explicit new owner instruction required after F-10 |

## 2026-07-17 F-10C2 current overlay

- **VALIDATED:** all nine frozen zero-lepton massless diagonal catalogue rows
  execute. Primary-tagged normalization, explicit six-species multiplicity,
  separate `K_s/K_t` topology, equilibrium/null, number/energy conservation,
  entropy, `T^5` scaling, analytic response/Jacobian, and the explicit seven-
  folded-channel sum pass. The final Rust release gate is `230/230` plus zero
  doctests.
- **VALIDATED:** the exact solver-local BDF Jacobian cache changes the matched
  dual-solver endpoint wall `2191.66 -> 968.38 s` (`55.815%`, `2.263x`) with
  bitwise-identical outputs/steps and `+1.229%` RSS. Four-topology production
  aggregation changes the completed-catalogue command wall
  `1520.92 -> 1184.77 s` (`22.102%`, `1.284x`). No approximate, cross-step,
  global, or policy-controlled reuse exists.
- **IMPLEMENTED:** the final private endpoint gives BDF
  `(N,t,N_eff)=(7.936693339485084,52677.63448707955 s,3.034035983584400)`,
  steps `1459/1`, and Rodas5P
  `(7.936706017467941,52678.09666722039 s,3.033904967773792)`, steps `271/29`.
  Solver-isolated final wall was not measured, so no exact current BDF/Rodas
  wall ratio is claimed.
- **DEPRECATED:** this historical overlay selected FortEPiaNO 1.4.0 as the
  structurally independent formulation/integrator, but D-025 supersedes that
  selection. Whole-program FortEPiaNO 1:1 authority is outside the frozen
  no-QKE objective; only its hash-locked classical diagonal initial-collision
  slice remains contextual.
- **FORBIDDEN:** same-code solver parity, green tests, or canonical `N_eff`
  proximity may not be promoted to independent/precision authority. F-11,
  QKE/coherence, public dispatch, and public production remain outside scope.

F-01 adds no Python binding, backend key, public API, BBN output, or
runtime-authority claim. Its pinned `diffsol` BDF uses the nalgebra linear
solver; faer is enabled only because the pinned release's nalgebra-only feature
surface does not compile locally. F-02 is a private two-leg ideal model:
common EM+neutrino entropy above `T_dec`, an exact event/restart, then EM-only
entropy below it. It excludes QED, baryon rest energy, nuclear backreaction,
collisions, and shear.

## Reachable-history diagnosis

The earliest reachable commit is
`692f394149846033fb39e974221db7d41b33e50f` (2026-04-20), a 646-file,
101,585-insertion snapshot already containing 40 JAX files and 15,557 JAX
lines. No earlier parent or configured remote is available. Development
therefore cannot be evidenced as a minimal JAX-to-BBN bottom-up sequence.

The observed programme extended a mature Python/JAX implementation and then
ported selected vertical and collision slices to Rust before Rust owned a
minimal isotropic BBN endpoint. The execution order is now corrected to
solver -> FLRW -> Born weak -> named minimal network -> coupled endpoint.

## Preserved historical slices

The following evidence remains accepted exactly within its old ceiling, but
none of these rows may select the next implementation:

| Slice | State | Boundary |
|---|---|---|
| PUB-00 / PUB-01 | CLOSED | inference/release path only; gold/physics RED |
| PUB-02C | CLOSED_WITH_LIMITS | NumPy/JAX Rodas5P primitive only |
| PORT-00 | CLOSED | Rust-first authority decision only |
| R-01 | CLOSED_WITH_LIMITS | private collisionless Tier-1 port fidelity |
| R-02A / R-02B | CLOSED_WITH_LIMITS | catalogue/units and author-HM event algebra |
| R-03A / R-03B1T | CLOSED_WITH_LIMITS | Pauli and supplied-event action algebra |
| R-03B2R / R-03B2B-R3 | CLOSED_WITH_LIMITS | scalar/generalized supplied response |
| R-03B3-R1 | ABANDONED_PREIMPLEMENTATION | no authority |
| R-03C1 / R-03C1A | ABANDONED_PREFALSIFIER | no implementation authority |
| R-03C1A-R1 | CLOSED_WITH_LIMITS | supported-point density and construction invariants |

R-03C1B and subsequent electron collision integration are PARKED until F-09,
after a minimal and matched standard BBN endpoint exists.

## Foundation gate status

Resolved within narrow claim ceilings:

1. F-01 now has a crate-private dual BDF/Rodas5P contract with directioned
   refined events, exact attempt/stat accounting, raw failures, stiff exact and
   Robertson checks, and deterministic repeat checks.
2. F-02 replaced GL64 port parity with a threshold-regularized continuum EOS,
   replayed the `2.4509557e-5` total-rho discrepancy at 0.1 MeV, and validates
   both entropy branches with a `T_dec` event/restart.
3. F-03 implements all six physically named tree-level charged-current
   channels with analytic lifetime normalization and threshold/tail-resolving
   quadrature, then consumes them in a two-leg standalone `X_n` freeze-out.
   Independent SciPy integrations and both Rust solvers agree at the declared
   `T_gamma=0.05 MeV` endpoint.
4. F-04 fixes the internal state order independently of both embedded JSON
   layouts, binds all 12 rows by physical identity, proves the standalone-table
   permutation, and validates the shared mass-fraction flux/RHS/Jacobian kernel
   with conservation, detailed-balance, boundary, and manufactured solves.
5. F-05 couples those substrates in one `N=ln(a)` state with explicit
   `d ln(n_b)/dN=-3`, a late-time eta anchor, exact-zero weak-only nuclear
   handoff, and raw state-domain/event failure. Both Rust solvers and an
   independent `T_gamma`-variable SciPy Radau/BDF formulation agree at the
   simplified endpoint; full release repeats are bitwise deterministic.
6. F-06 serializes and consumes one exact matched profile, agrees blockwise
   and at the endpoint with live PRIMAT v0.3.2, checks the independent
   RHS/integrator through LINX v0.1.2 with shared inputs declared, measures the
   full matched endpoint, and removes the superseded public JAX endpoint
   surface. Active source is `65,377` lines versus the reconstructed PORT-00
   implementation baseline `68,268`.
7. F-07 preserves the F-06 zero branch bitwise, derives all scalar QED
   thermodynamics from one pressure, independently checks `dPa`, `dPb`, and
   `dPe3`, and reproduces the live PRIMAT leading-QED endpoint delta. The
   complete endpoint passes both Rust solvers and conservation, but lacks an
   external endpoint oracle and has a material direct-quadrature cost.
8. F-08A preserves the Born branch bitwise, implements PRIMAT's combined
   zero-temperature Coulomb/resummed-radiative model in all six channels and
   its own lifetime normalization, and consumes it in standalone freeze-out
   and the leading-QED matched endpoint. Adaptive component integrations, live
   PRIMAT C/Python endpoints, and an independent `T_gamma`-variable DOP853
   solve with nested QED grids agree with both Rust solvers.
9. F-08B adds the first-order-in-`1/M_N` PRIMAT Fokker-Planck correction to
   the CCR integrands and lifetime normalization rather than applying a scalar
   rate factor. It fixes `delta_kappa=0`, assigns `m_p/m_e` to `n->p` and
   vacuum normalization and `m_n/m_e` to `p->n`, and uses direction-specific
   quadrature scales after an extreme-temperature-ratio falsifier exposed a
   tiny-channel error. Compact/expanded and adaptive component checks, a
   separate `T_gamma`-variable DOP853 solve, Rust BDF/Rodas endpoints, and a
   live matched PRIMAT Python endpoint pass. PRIMAT C is deliberately SKIPPED
   because it cannot represent this no-weak-magnetism profile. The final Rust
   release gate passes 142/142 all-target tests plus 0 doctests; the broader
   Python bundle retains the inherited public-SciPy CL2 AlterBBN RED rather
   than hiding or repairing it.
10. F-08C activates the physical PRIMAT anomalous weak-magnetism coefficient
    `delta_kappa=3.70589007463` through that same generalized evaluator while
    preserving the exact F-08B zero-coefficient branch. Independent component
    anchors, the nonzero `3.6755293490786656e-5` first-order detailed-balance
    residual, the GL320 weak-only standalone solve, fresh two-repeat PRIMAT
    v0.3.2 Python/C matched endpoints, and Rust BDF/Rodas endpoints pass. The
    external validator reports 23/23 and rejects 5/5 mutation probes. The
    first standalone GL ladder was RED at `4.0784416011074853e-8`; physical
    panel splitting reduced GL160/320 `X_n` refinement to
    `9.259088912250135e-9` without widening the frozen ceiling. Final Rust
    release tests pass 150/150 with strict Clippy and formatting clean; the
    stored compact replay passes, unsupported live mode exits `2`, and its
    hardened exact locks reject coordinated component/rate/freeze-out/endpoint
    drift, including coordinated C-endpoint drift.
11. F-08D adds the complete true-photon, differential-bremsstrahlung, `L1`,
    and `L2+3` finite-temperature radiative terms as one signed directional
    correction over F-08C, with the unchanged physical-WM lifetime
    normalization. Physical electron/photon panel boundaries closed an honest
    low-temperature tensor-quadrature RED without changing the formula or
    frozen envelopes. Six direct complete anchors, the independently resolved
    `2e8 K` n-to-p decomposition, all 56 private-table intervals in both
    directions, the finite 10 MeV Jacobian, dual consumers, matched endpoint,
    and deterministic repeat pass. External evidence reports 25/25 and 5/5
    mutation rejection. The final Rust gate passes 160/160 plus 0 doctests and
    strict static checks. Precision promotion remains BLOCKED: the standalone
    Rust-versus-C-knot comparator misses the frozen `8e-8` `X_n` budget by
    `O(1e-6)`, and the C global/local interpolation spread alone moves `Yp` by
    `1.0021545698846168e-6`.
12. F-08N preserves the first 12 reactions and binds Rabbit's selected 31-row
    AC2024 table by identity, consumes stored reverse coefficients verbatim,
    evolves Li6, and reaches the corrected endpoint with BDF, Rodas5P, and a
    tighter BDF repeat. The pre-Rust PRIMAT campaign passes 46/46 checks and
    5/5 external mutations; Rust release passes 167/167 plus 0 doctests. The
    consolidated replay rejects undeclared claim fields and rehashes the
    repo-local source table. This is central-rate, custom-subset evidence only:
    `sigma` uncertainty arrays are not propagated, all 31 rows activate at the
    existing F-06 handoff rather than PRIMAT's 17-then-31 staging, and F-08D's
    strict standalone RED remains open.
13. F-09A adds the electron-only massless Maxwell--Boltzmann energy-transfer
    moment, a finite-electron-mass leading-QED three-temperature FLRW consumer,
    and a crate-private F-08C weak plus selected-31 cold endpoint. Exact null,
    sign, coupling multiplicity, entropy production, combined first-law closure,
    independent QED-off FLRW integration, BDF/Rodas5P, full-leg tolerance
    refinement, coupled-Jacobian five-point parity, and same-binary release repeat
    pass. This validates the stated mixed thermal-moment model only; it is not a
    non-test runtime path or an independently validated QED-on BBN prediction.
14. F-09B replaces the live private-endpoint source with a finite-vacuum-
    electron-mass FD/Pauli tree-level energy moment in plasma-frame/CM
    coordinates. Independent production-component anchors, a five-level
    quadrature ladder, the direct HM support/root construction oracle, the
    analytic massless-MB limit, null/sign/entropy/cold suppression, event
    energy and channel number/lepton topology, combined first law, an
    independent QED-off DOP853/Radau endpoint, Rust BDF/Rodas, tighter BDF,
    coupled Jacobian, conservation, and bitwise repeat pass. The evidence is
    thermal-isotropic and test-private. Its original production rule exposed
    a material repeated-run endpoint wall that F-10B0 subsequently reduces.
15. F-10A fixes `q=a p`, evolves an arbitrary bounded isotropic occupation
    vector with exact collisionless `df/dN=0`, and feeds its energy density into
    the Friedmann rate and cosmic-time state through the existing photon-
    temperature event. FD moment resolution, arbitrary-distribution comoving
    number/energy invariants, analytic Jacobian and explicit-`N` derivative,
    raw invalid states, dual solvers, unchanged occupation bits, and an
    independent continuum-FD SciPy endpoint pass. This is the collisionless
    energy-grid substrate only, not a collision or distortion result.
16. F-10B0 reduces the existing direct thermal CM rule from polar six to
    polar four while retaining radial six and azimuth eight. A six-temperature,
    two-flavour executable envelope stays below `2e-5`; independent polar
    three is rejected at `4.69e-5/7.63e-5`. The actual selected-31 BDF/Rodas
    cold legs improve from `123.71/393.10 s` to `54.21/181.73 s`, and all
    198 release tests pass. This moves the pre-node performance prerequisite;
    it does not add a spectral collision operator.
17. F-10B1 constructs the finite-mass HM electron/positron action at each
    comoving momentum node, conservatively projects elastic and pair event
    fluxes, folds the Pauli occupation Jacobian, and consumes its energy moment
    in a private QED-off FLRW endpoint. Component normalization reaches the
    independent thermal anchor at order 32/24, but the order-six endpoint is
    coarse and order eight/six fails the frozen dual-solver nodal envelope.
18. F-10B2 stores the spectral state in exact logit coordinates, so finite
    solver iterates reconstruct strict open occupations without clipping and
    unrepresentable underflow fails raw. A decomposed grid/event ladder rejects
    event four/three, selects grid 16 with event six/four, and checks a
    same-code BDF grid-20 refinement plus a dual-solver grid-16 endpoint. The
    retained thermal moments are within one percent of the independent F-09B
    anchors; an explicit-`N` support step ladder remains in the executable
    gate. This is a resolution-selected private regression, not an independent
    precision spectral solution.
19. F-10C0 implements only same-flavour massless diagonal
    `nu_a nu_a <-> nu_a nu_a` and the identical-antineutrino copy. Its global
    full-event coefficient 16 reproduces the tagged target-row coefficient 64
    across four conservative legs. Nonnegative linear interpolation/deposition,
    Pauli gain/loss, eventwise number/energy identities, affine FD nulls,
    entropy sign, `T^5` scaling, and an analytic logit Jacobian pass focused
    checks. The component reaches both private FLRW solvers and the final
    217/217 release/static gate passes, but selected GL16 radial resolution was
    RED at that historical boundary.
20. F-10C1 retains the nonnegative linear basis and selects the fixed positive
    map `y=-3 ln(1-t)` at N48. The direct N32/40/48/64 ladder decreases through
    `1.873525%/1.227393%/0.852441%/0.489704%`, while two independent N128
    auxiliary rules bound five declared nonthermal state profiles by a worst
    `1.9771546% < 2.1%`. Lower composite N32, exponential N32, and exponential
    N40 candidates are preserved as RED. This closes only the declared radial
    representation boundary with limits; it does not add a catalogue row.

Remaining blockers:

1. F-10C validates only the same-flavour identical-neutrino CM component and
   the declared F-10C1 N48 five-profile radial envelope. The direct scalar
   weak-action ladder is necessary but not sufficient; the independent
   auxiliary state-basis envelope controls selection. Catalogue row 1 is
   complete and the same-flavour-identical subset of row 3 executes; the
   distinct-heavy remainder of row 3 and rows 2 and 4--9 remain absent. Thus
   row 3 is partial and seven rows are wholly absent. An independent full spectral endpoint,
   arbitrary-distribution/continuum authority, collision-radiative corrections,
   precision decoupling, and repeated-run performance authority remain absent.
2. F-08D strict standalone precision authority remains BLOCKED. Its direct
   Rust table and the temporary noisy C-knot/global-quadratic comparator differ
   by `O(1e-6)` in `X_n` against a frozen `8e-8` ceiling; C global/local
   interpolation moves `Yp` by `1.0021545698846168e-6`. The conditional
   complete-block and broad matched-endpoint evidence does not erase this RED.
3. The accepted corrected profile remains bounded to Rabbit's selected
   central-rate 31-row table, first-order finite-mass expansion, weak-only
   standalone solve, and a thermal-isotropic electron collision closure.
   Rate-uncertainty propagation, independently validated spectral/neutrino-neutrino transport,
   collision radiative corrections, QKE, and precision/public-production
   authority remain absent.
4. At artificial `T_gamma/T_nu=1000`, default GL64 is not precision authority
   for a negligible individual capture channel: it gives
   `2.7587807268e-8` versus the threshold-split adaptive
   `2.7587184001e-8` (`2.26e-5` relative), although totals are stable and
   GL320 reaches `9.2e-7` relative. This LOW limitation does not reopen the
   matched endpoint.
5. F-07 complete-QED repeated-endpoint cost remains material, and no external
   complete `dPb` endpoint oracle is available.
6. The current gold lane has inherited failures and does not validate the new
   foundation.

## Evidence and claim boundary

### DERIVED

- The reachable history is migration/translation-first rather than a Rust
  bottom-up construction.
- The new dependency order removes collision work from the critical path until
  a standard endpoint can consume it.

### IMPLEMENTED

- Accepted historical private Rust collisionless and collision-algebra
  substrates listed above.
- F-01 dual solver, F-02 fixed-owner FLRW/EOS, F-03 Born-rate/freeze-out, and
  F-04 named minimal-network substrates are crate-private implementation.
- F-05 is the first shared crate-private consumer: one coupled state, explicit
  baryon-density history, exact-zero handoff, signed internal-stage extension,
  raw accepted-state failures, terminal observables, and solver statistics.
  None of F-01--F-06 is wired into public Python dispatch.
- F-06 adds the matched high-temperature entropy branch, Saha handoff,
  log-positive nuclear integration, serialized configuration/results, and
  hard retirement of the superseded high-level JAX endpoint surface.
- F-07 adds crate-private `Off`, `PrimatLeadingE2E3`, and
  `PrimatCompleteE2E3` scalar EOS modes and threads them through FLRW,
  standalone freeze-out, entropy-normalized baryon density, and the minimal
  matched endpoint. The complete endpoint remains IMPLEMENTED, not externally
  validated.
- F-08A adds crate-private `Born` and `PrimatZeroTemperatureCcr` weak-model
  selection in the existing consumer path. CCR is one inseparable model with
  its own lifetime normalization; Coulomb-only and radiative-only endpoint
  modes were not added.
- F-08B adds crate-private
  `PrimatZeroTemperatureCcrFiniteMassNoWeakMagnetism`, its additive
  Fokker-Planck channel and lifetime terms, and consumers in standalone
  freeze-out and the leading-QED matched endpoint. The anomalous
  weak-magnetism coefficient is exactly zero.
- F-08C adds crate-private
  `PrimatZeroTemperatureCcrFiniteMassPhysicalWeakMagnetism` with
  `delta_kappa=3.70589007463`. F-08B and F-08C share one generalized
  finite-mass evaluator, lifetime normalization, and endpoint consumers; the
  physical term is not a scalar multiplier.
- F-08D adds crate-private
  `PrimatCompleteThermalRadiativePhysicalWeakMagnetism`, one direct four-term
  signed directional evaluator, a private 57-point in-memory table, and the
  existing standalone and matched consumers. Pre-F-08D selectors retain exact
  zero thermal directional fields; no partial-term endpoint selector or public
  dispatch was added.
- F-08N adds crate-private `NetworkExtent::Selected31`, identity-bound table
  loading, the unchanged 12-row prefix, Li6 evolution, and the existing
  corrected endpoint consumer. Stored reverse coefficients are consumed
  verbatim; raw rounded Q values do not reconstruct reverse-rate authority.
- F-09A adds crate-private `ElectronThermalTransfer`, a three-temperature FLRW
  RHS, and a thermal weak/background/network vertical slice. The collision
  source is consumed to `T_gamma=0.005 MeV` only through internal Rust tests;
  no Python binding, public backend key, or non-test runtime entrypoint is added.
- F-10C0 adds a crate-private same-flavour neutrino/antineutrino self-collision
  action and threads it through the existing logit state, analytic Jacobian,
  and dual-solver isotropic FLRW consumer. This completes catalogue row 1 and
  the same-flavour-identical subset of row 3; the distinct-heavy part of row 3,
  rows 2 and 4--9, and all QKE/off-diagonal state remain absent.
- F-10C1 replaces only that private consumer's historical GL16 radial grid with
  the fixed positive N48 `y=-3 ln(1-t)` representation, shared between the
  endpoint and the executable collision tests. A prepared same-flavour event-
  geometry candidate was measured and removed after its `9.195%` cold-wall
  reduction missed the policy threshold and increased RSS; no cache, channel
  formula, public dispatch, runtime policy, or catalogue row is added.

### VALIDATED

- F-01 solver mechanics only: nonautonomous/order, stiff exact, Robertson,
  event direction/refinement, raw failure, budget, and determinism contracts.
- F-02 only for its declared idealized tree-level FLRW model: independent
  continuum EOS, limits, entropy ownership, two-leg cooling, Friedmann units,
  and BDF/Rodas terminal agreement.
- F-03 only for the declared uncorrected six-channel Born rates and standalone
  neutron freeze-out: independent continuum anchors, detailed balance in its
  valid equal-temperature limit, quadrature convergence, lifetime/temperature
  limits, raw failures, and independent plus dual-solver endpoint agreement.
- F-04 only for the isolated nuclear kernel: named table identity/remap,
  stoichiometry, rate interpolation, density/symmetry powers, analytic
  abundance Jacobian, reaction permutation, raw failures, local balance,
  conservation, boundary state, and shared-kernel manufactured solutions.
- F-05 only for the explicit simplified coupled model: independent fixed-RHS
  and endpoint reconstruction, BDF/Rodas agreement, tolerance/quadrature
  convergence, density and nuclear-suboperator conservation accounting,
  exact-zero initialization, raw domain failures, and deterministic endpoint
  repeats.
- F-06 only for the exact serialized zero-QED/Born/12-reaction matched profile:
  live PRIMAT block/endpoint agreement, LINX shared-input RHS/integrator
  comparison, dual-solver conservation, raw failure, and repeated endpoint
  evidence. Rust is its private number-of-record.
- F-07 scalar components: independent continuum anchors, analytic-versus-
  finite-difference pressure derivatives, thermodynamic identities, limits,
  exchange-quadrature convergence, and exact zero-branch preservation. The
  leading `dPa+dPe3` endpoint delta is independently checked by live PRIMAT;
  `dPb` is independently checked pointwise and in its first two temperature
  derivatives.
- F-08A only for the declared zero-temperature CCR block and exact matched
  profile: independent physical-channel and lifetime-normalization anchors,
  all-six-channel totals, detailed balance, limits, split-quadrature
  convergence, live PRIMAT C/Python endpoint deltas, independent
  `T_gamma`-DOP853 standalone freeze-out, dual Rust solvers, determinism, and
  raw failures. LINX is formula-reviewed only for F-08A; no numerical LINX
  execution is claimed.
- F-08B only for the exact matched no-weak-magnetism profile: independent
  compact-versus-expanded integrands, adaptive component/rate and lifetime
  anchors, all-six-channel totals, direction-mass ownership, quadrature and
  raw-failure checks, the deliberately nonzero modified-detailed-balance
  residual, a separate `T_gamma`-DOP853 standalone solve, Rust BDF/Rodas, and
  the live PRIMAT v0.3.2 Python custom-table endpoint. The external validator
  passes 17/17 and the full Rust release gate passes 142/142 plus 0 doctests;
  the PRIMAT C backend was not run.
- F-08C only for the exact matched physical-weak-magnetism profile:
  compact-versus-expanded component anchors, physical couplings and lifetime
  normalization, six channels, total rates, direction ownership, limits,
  quadrature and raw failures, the deliberately nonzero
  `3.6755293490786656e-5` modified-detailed-balance residual, a GL320
  `T_gamma`-variable weak-only solve, fresh two-repeat PRIMAT v0.3.2 Python/C
  custom-table endpoints, and Rust BDF/Rodas endpoints. The external validator
  passes 23/23, all 5/5 mutation probes reject, and the final Rust release gate
  passes 150/150 with both focused F-08C consumer tests and static gates clean.
- F-08D only for its complete four-subterm directional block, private table,
  and conditional 12-reaction endpoint: six pre-Rust complete anchors, the
  independently resolved n-to-p subterm anchor, GL64/128 refinement, all 56
  interval midpoints in both directions, raw limits/failures, finite boundary
  Jacobian, dual Rust consumers, bitwise repeat, broad external endpoint
  budgets, and isolated negative abundance shifts pass. The external validator
  passes 25/25 and rejects 5/5 mutations; stored replay passes. The strict
  standalone `X_n` precision comparison remains BLOCKED and is not promoted by
  the conditional endpoint evidence.
- F-08N only for Rabbit's selected 31-row central-rate topology and the exact
  all-31-from-F06-handoff matched profile: identity/reference/topology checks,
  independent rate/RHS/Jacobian anchors, nested 31-minus-12 effects, external
  46/46 validation and 5/5 mutation rejection, conservation, tolerance
  refinement, BDF/Rodas agreement, deterministic repeat, 167/167 release
  tests, closed replay schemas, and live source-table rehash pass.
- F-09A only for its declared massless-MB electron thermal moment and mixed 3T
  private endpoint: analytic/decimal anchors, null/sign/scaling/multiplicity,
  entropy sign, first-law closure, independent QED-off FLRW endpoints,
  coupled-Jacobian five-point parity, dual solvers, tightened full-leg tolerance
  refinement, positivity/conservation, and same-binary release repeat pass.
- F-10C0 only for the same-flavour massless diagonal CM component: focused
  `8/8` checks cover tagged MB normalization, common/affine FD nulls,
  eventwise number/energy conservation, entropy sign, `T^5`, analytic logit
  response, raw failures, and high-order production-to-target convergence.
  The historical selected GL16 endpoint is excluded from that component
  resolution-validation claim.
- F-10C1 only for the selected radial representation over the five declared
  smooth nonthermal profiles. The durable direct and two-N128-auxiliary Rust
  tests, independent hash-locked replay, FD moments, electron anchors,
  conservation, entropy, and constructor lock pass with worst state-basis
  residual `1.97715455697% < 2.1%`. This does not validate arbitrary
  distributions, continuum truth, the incomplete/absent catalogue parts, or a precision
  decoupling endpoint.
- No stock/full-network, rate-uncertainty, finite-mass/FD collision,
  independently checked QED-on precision BBN abundance, standard `N_eff`,
  spectral transport, LRS, non-LRS, public Rust backend, or public-production
  claim is promoted.

### FORBIDDEN

- treating solver tests as BBN validation;
- treating Rust/JAX parity as independent physics validation;
- using the standalone 12-reaction file by positional index;
- clipping negative abundances or repairing failed endpoints;
- calling the F-05 `0.08 MeV` activation plateau-converged or externally
  matched;
- calling the complete `dPa+dPb+dPe3` endpoint externally validated;
- extending the F-08A zero-temperature CCR validation to finite mass, weak
  magnetism, thermal weak corrections, the full network, collisions,
  anisotropy, or public production;
- calling the F-08B no-weak-magnetism custom profile stock PRIMAT,
  precision-standard, C-backend validated, physical-weak-magnetism validated,
  or evidence for thermal weak terms, the full network, transport, QKE,
  anisotropy, or public production;
- calling the F-08C matched custom-table result stock PRIMAT,
  precision-standard, finite-temperature-weak complete, expanded-network,
  transport/collision, anisotropic, QKE, public-runtime, or public-production
  validation;
- calling the F-08D conditional 12-reaction endpoint a precision weak table,
  stock PRIMAT prediction, expanded-network convergence, corrected general
  Rust number-of-record, transport/collision, anisotropic, QKE, public-runtime,
  or public-production validation;
- calling the F-08N custom selected subset a stock PRIMAT network, treating
  shared central rates as measurement validation, claiming `sigma` uncertainty
  propagation, erasing the F-08D standalone RED, or promoting collision,
  transport, anisotropic, QKE, public-runtime, or public-production authority;
- calling F-09A a finite-electron-mass or FD/Pauli collision validation, an
  independently validated QED-on abundance/`N_eff` prediction, a spectral
  Boltzmann solution, a non-test runtime endpoint, or public production;
- calling F-10C0 a resolution-converged selected-grid endpoint, complete
  classical neutrino--neutrino catalogue, precision decoupling result,
  Type-I/QKE validation, runtime promotion, or public production;
- new JAX runtime development, QKE, complete-SM, Rust production authority, or
  public-production claims.

## Worktree and cost boundary

The user-owned `pyproject.toml` modification and unrelated untracked files are
outside this programme write set and must be preserved. `native/rabbit_cpu/target/`
also pre-existed as an untracked build surface; new validation uses a temporary
target directory.

F-00 is a consolidation exception: it deletes substantially more normative
planning surface than it adds. Its final policy cost line is recorded below.
F-01's authored Rust and generated Cargo lock changes are reported separately
at closeout.

```text
added_lines: 676
deleted_lines: 2811
net_lines: -2135
files_touched: 6
token_use_exact: UNAVAILABLE
token_use_basis: the harness exposes thread-level use, not a reliable F-00-scoped counter
runtime_behavior_changed: no
physics_behavior_changed: no
known_blocker_reduced: no
blocker_movement_ratio: 0.00
validation_strengthened: yes
cost_effectiveness_verdict: ACCEPT_WITH_LIMITS
```

The verdict reflects large net consolidation and a clearer executable order,
not movement of an endpoint, runtime, solver, or physics blocker.

F-01/F-02 were implemented in one uncommitted working-tree wave, so their
shared ledger cost is reported as one policy line rather than pretending a PR
boundary. The generated lockfile is excluded from this authored/net count and
reported separately as `+1551/-107/net +1444`.

```text
added_lines: 2170
deleted_lines: 0
net_lines: 2170
files_touched: 10
token_use_exact: UNAVAILABLE
token_use_basis: the harness exposes thread-level use, not a reliable F-01/F-02-scoped counter
runtime_behavior_changed: yes
physics_behavior_changed: yes
known_blocker_reduced: yes
blocker_movement_ratio: 0.50
validation_strengthened: yes
cost_effectiveness_verdict: ACCEPT_WITH_LIMITS
```

The `>400`-line exception is explicit: a dual implicit-solver contract plus
its failure/conformance suite and an independently checked EOS/entropy endpoint
could not be split without leaving the named F-01/F-02 blockers open. The
result is still local progress, not a full BBN endpoint. Through F-02,
cumulative Rust source addition since PORT-00 is 5,102 lines and substantive
superseded Python/JAX source deletion is zero; F-06 deflation remains open.

F-03 is reported as one wave because its standalone endpoint is the first
consumer of the six-channel kernel. Generated-lockfile change is zero for this
wave. The `>400`-line exception covers the six explicit channel formulas,
threshold/tail quadrature, independent reference grid, and the first physical
solver consumer; splitting them would leave the Born normalization blocker or
its endpoint consumption untested.

```text
added_lines: 1172
deleted_lines: 48
net_lines: 1124
files_touched: 10
token_use_exact: UNAVAILABLE
token_use_basis: the harness exposes thread-level use, not a reliable F-03-scoped counter
runtime_behavior_changed: yes
physics_behavior_changed: yes
known_blocker_reduced: yes
blocker_movement_ratio: 0.50
validation_strengthened: yes
cost_effectiveness_verdict: ACCEPT_WITH_LIMITS
```

The count is the F-03 stage delta: 1,057 new Born/freeze-out source lines, two
module declarations, an 18-line net fixed-owner FLRW expansion consumed by the
freeze-out system, and the six existing ledger/plan deltas (`+95/-48`). Through
F-03, cumulative Rust source addition since PORT-00 is 6,179 lines and
substantive superseded Python/JAX source deletion remains zero. That debt is
accepted only until F-06 source deflation; F-03 is not endpoint authority.

F-04 is a physical-kernel exception to the `>400`-line default: its one module
contains the immutable species/reaction catalogue, both embedded-table parsers,
raw rate/flux/Jacobian implementation, and executable conservation and
manufactured-solution evidence. Splitting those contracts would recreate the
positional/data-provenance gap the stage was required to close. The direct
`serde_json` dependency added one generated lock root-entry line but no new
package/version.

```text
added_lines: 1394
deleted_lines: 48
net_lines: 1346
files_touched: 10
token_use_exact: UNAVAILABLE
token_use_basis: the harness exposes thread-level use, not a reliable F-04-scoped counter
runtime_behavior_changed: yes
physics_behavior_changed: yes
known_blocker_reduced: yes
blocker_movement_ratio: 0.50
validation_strengthened: yes
cost_effectiveness_verdict: ACCEPT_WITH_LIMITS
```

The authored count comprises 1,297 new network source lines, one module
declaration, one direct dependency line, and existing plan/ledger changes
including this cost record (`+95/-48`). Generated lock delta is separately
`+1/-0`. Through F-04, cumulative Rust source addition since PORT-00 is 7,477
lines; substantive superseded Python/JAX deletion remains zero and F-06 owns
that open debt. No endpoint or final-abundance authority is claimed.

F-05 is an endpoint-blocker exception to the `>400`-line default. The coupled
module, solver-domain contract, and finite signed network-stage extension had
to land together to preserve exact-zero initialization without clipping while
still rejecting every invalid accepted/event state. A frozen F-04 Git blob was
not created for the untracked Rust sources. Exact stage-start and stage-end
line counts recover an exact Rust-source net of `+1353` (`ode +206`, network
`+194`, new coupled module `+952`, module declaration `+1`), but they cannot
recover an honest added/deleted split after the intervening formatter and
refactors. That split is reported unavailable rather than estimated.

```text
added_lines: UNAVAILABLE (Rust split unrecoverable; existing-document delta +106)
deleted_lines: UNAVAILABLE (Rust split unrecoverable; existing-document delta -0)
net_lines: 1459
files_touched: 10
token_use_exact: UNAVAILABLE
token_use_basis: the harness exposes thread-level use, not a reliable F-05-scoped counter
runtime_behavior_changed: yes
physics_behavior_changed: yes
known_blocker_reduced: yes
blocker_movement_ratio: 1.00
validation_strengthened: yes
cost_effectiveness_verdict: ACCEPT_WITH_LIMITS
generated_lockfile_lines: +0/-0/net 0
cumulative_rust_source_additions: 8830
superseded_python_jax_deletions: 0
endpoint_progress: MEASURED
```

The exact net is `1353` Rust-source lines plus `106` lines across the six
existing plan/ledger files relative to their frozen F-04 numstat. The
unavailability is limited to the Rust authored addition/deletion split; the
exact source net and document delta remain reproducible. F-05 directly moves
the missing executable-endpoint blocker, but its lack of source deflation and
matched external authority prevents an unconditional acceptance. F-06 owns
both debts.

F-06 is the source-deflation and matched-endpoint exception. Its F-05 boundary
was not frozen as a Git blob, so an honest authored added/deleted split and
stage-local file count cannot be reconstructed. The exact active
implementation total is nevertheless reproducible: `55,860` Python plus
`9,517` Rust = `65,377`, which is `2,891` lines below the reconstructed
PORT-00 baseline of `68,268`.

```text
added_lines: UNAVAILABLE (F-05/F-06 uncommitted boundary was not frozen)
deleted_lines: UNAVAILABLE (F-05/F-06 uncommitted boundary was not frozen)
net_lines: -2891 active implementation lines versus PORT-00
files_touched: UNAVAILABLE at the F-06 stage boundary
token_use_exact: UNAVAILABLE
token_use_basis: the harness exposes thread-level use, not a reliable F-06-scoped counter
runtime_behavior_changed: yes
physics_behavior_changed: no for the matched minimal model; public dispatch changed
known_blocker_reduced: yes
blocker_movement_ratio: 1.00
validation_strengthened: yes
cost_effectiveness_verdict: ACCEPT_WITH_LIMITS
generated_lockfile_lines: +0/-0/net 0 during F-06
cumulative_rust_source_lines: 9517
superseded_python_jax_deletions: substantive endpoint/runtime source and tests deleted
endpoint_progress: MEASURED
```

F-07 directly closes the scalar-QED correctness blocker but leaves a measured
complete-mode cost and no external `dPb` endpoint oracle. The recorded F-06
Rust total makes the exact Rust-source net reproducible (`10,620-9,517=+1,103`),
but the other existing surfaces were not frozen between the uncommitted F-06
and F-07 waves, so their stage-local addition/deletion split is unavailable.

```text
added_lines: UNAVAILABLE (exact Rust-source net +1103; other stage split not frozen)
deleted_lines: UNAVAILABLE (exact Rust-source net +1103; other stage split not frozen)
net_lines: UNAVAILABLE for all authored surfaces; exact Rust-source net +1103
files_touched: 13
token_use_exact: UNAVAILABLE
token_use_basis: the harness exposes thread-level use, not a reliable F-07-scoped counter
runtime_behavior_changed: yes
physics_behavior_changed: yes
known_blocker_reduced: yes
blocker_movement_ratio: 0.75
validation_strengthened: yes
cost_effectiveness_verdict: ACCEPT_WITH_LIMITS
generated_lockfile_lines: +0/-0/net 0
cumulative_rust_source_lines: 10620
superseded_python_jax_deletions: 0 in F-07
endpoint_progress: MEASURED
```

F-08A directly closes the missing zero-temperature CCR correctness and
endpoint-consumption blocker. Its uncommitted F-07 boundary did not freeze all
authored surfaces, so an honest added/deleted split remains unavailable. The
exact Rust-source net is reproducible (`11,701-10,620=+1,081`). Conservative
active implementation source is `55,860` Python plus `11,701` Rust = `67,561`,
still `707` lines below the PORT-00 baseline `68,268`.

```text
added_lines: UNAVAILABLE (exact Rust-source net +1081; other stage split not frozen)
deleted_lines: UNAVAILABLE (exact Rust-source net +1081; other stage split not frozen)
net_lines: UNAVAILABLE for all authored surfaces; exact Rust-source net +1081
files_touched: 11
token_use_exact: UNAVAILABLE
token_use_basis: the harness exposes thread-level use, not a reliable F-08A-scoped counter
runtime_behavior_changed: yes
physics_behavior_changed: yes
known_blocker_reduced: yes
blocker_movement_ratio: 0.75
validation_strengthened: yes
cost_effectiveness_verdict: ACCEPT_WITH_LIMITS
generated_lockfile_lines: +0/-0/net 0
cumulative_rust_source_lines: 11701
superseded_python_jax_deletions: 0 in F-08A
endpoint_progress: MEASURED
```

The `>400`-line exception covers a singularity-safe six-channel correction,
model-consistent lifetime normalization, its endpoint consumer, and
independent component/standalone/live-PRIMAT evidence. No wrapper, readiness,
manifest, hash-gate, public dispatch, or JAX runtime surface was added. The
complete-QED performance blocker and all later correction/transport blockers
remain explicit.

F-08B directly closes the finite-nucleon-mass/no-weak-magnetism correctness
and endpoint-consumption blocker. Its exact Rust-source delta is reproducible
(`12,677-11,701=+976`), but the other uncommitted authored surfaces were not
frozen at the F-08A boundary. Conservative active implementation source is
`55,860` Python plus `12,677` Rust = `68,537`, which is `269` lines above the
PORT-00 baseline `68,268`. F-08B performs no substantive source deflation.

```text
added_lines: UNAVAILABLE (exact Rust-source net +976; other stage split not frozen)
deleted_lines: UNAVAILABLE (exact Rust-source net +976; other stage split not frozen)
net_lines: UNAVAILABLE for all authored surfaces; exact Rust-source net +976
files_touched: UNAVAILABLE at the uncommitted F-08B stage boundary
token_use_exact: UNAVAILABLE
token_use_basis: the harness exposes no reliable F-08B-scoped exact counter
runtime_behavior_changed: yes
physics_behavior_changed: yes
known_blocker_reduced: yes
blocker_movement_ratio: 0.75
validation_strengthened: yes
cost_effectiveness_verdict: ACCEPT_WITH_LIMITS
generated_lockfile_lines: +0/-0/net 0
cumulative_rust_source_lines: 12677
superseded_python_jax_deletions: 0 in F-08B
endpoint_progress: MEASURED
```

The `>400`-line exception covers the additive six-channel Fokker-Planck
algebra, direction-specific quadrature remediation, model-consistent lifetime
normalization, two Rust endpoint consumers, and independent component,
standalone, and live-PRIMAT-Python evidence. At the F-08B boundary the verdict
was `ACCEPT_WITH_LIMITS` because the active implementation had crossed above
the PORT-00 baseline, physical weak magnetism and the thermal/full-network
blocks were open, and no stock/precision/C claim was available.

F-08C closes the physical-weak-magnetism correctness and endpoint-consumption
blocker with fresh Python/C and dual-Rust evidence. Exact Rust source grows
`13,464-12,677=+787`. Python under `src/rabbit` remains `55,860`, so active
implementation source is `69,324`, or `1,056` lines above the PORT-00 baseline
`68,268`. No Python/JAX source was deflated.

```text
added_lines: UNAVAILABLE (exact Rust-source net +787; other F-08B boundary surfaces were not frozen)
deleted_lines: UNAVAILABLE (exact Rust-source net +787; other F-08B boundary surfaces were not frozen)
net_lines: UNAVAILABLE for all authored surfaces; exact Rust-source net +787
files_touched: 11 (3 Rust, fixture, replay script, 6 SSOT docs)
token_use_exact: UNAVAILABLE
token_use_basis: the harness exposes no reliable F-08C-scoped exact counter
runtime_behavior_changed: yes
physics_behavior_changed: yes
known_blocker_reduced: yes
blocker_movement_ratio: 0.75
validation_strengthened: yes
cost_effectiveness_verdict: ACCEPT_WITH_LIMITS
generated_lockfile_lines: +0/-0/net 0
cumulative_rust_source_lines: 13464
superseded_python_jax_deletions: 0 in F-08C
endpoint_progress: MEASURED
```

The verdict remains `ACCEPT_WITH_LIMITS`: F-08C moves a named physics blocker
through externally checked endpoint consumers, but active source remains above
PORT-00, no deflation occurred, and F-08D/F-08N/transport were still open or
parked at that boundary.

F-08D closes the missing complete finite-temperature radiative weak block and
its conditional endpoint consumer without splitting partial terms into runtime
modes. Exact Rust source grows `15,020-13,464=+1,556`. Python under
`src/rabbit` remains `55,860`, so active implementation source is `70,880`, or
`2,612` lines above the PORT-00 baseline `68,268`. No Python/JAX source was
deflated. The strict standalone `X_n` precision gate remains RED, so this is a
physics-correctness and conditional-endpoint closure, not precision promotion.

```text
added_lines: UNAVAILABLE (exact Rust-source net +1556; other F-08C boundary surfaces were not frozen)
deleted_lines: UNAVAILABLE (exact Rust-source net +1556; other F-08C boundary surfaces were not frozen)
net_lines: UNAVAILABLE for all authored surfaces; exact Rust-source net +1556
files_touched: 12 (4 Rust, fixture, replay script, 6 SSOT docs)
token_use_exact: UNAVAILABLE
token_use_basis: the harness exposes no reliable F-08D-scoped exact counter
runtime_behavior_changed: yes
physics_behavior_changed: yes
known_blocker_reduced: yes
blocker_movement_ratio: 0.75
validation_strengthened: yes
cost_effectiveness_verdict: ACCEPT_WITH_LIMITS
generated_lockfile_lines: +0/-0/net 0
cumulative_rust_source_lines: 15020
superseded_python_jax_deletions: 0 in F-08D
endpoint_progress: MEASURED
```

The `>400`-line exception covers the full coupled real/virtual four-subterm
integrals, singularity-aware quadrature, fail-closed direction-specific table,
two existing consumers, and independent direct/standalone/matched evidence.
No new public dispatch, readiness, manifest, hash-gate, figure, or JAX runtime
surface was added. The verdict is `ACCEPT_WITH_LIMITS`: a named physics blocker
and conditional endpoint moved, but active source grew, no deflation occurred,
and strict precision authority remains BLOCKED.

F-08N closes the selected-network identity and endpoint-consumption blocker.
Rust source grows `15,941-15,020=+921`; the consolidated replay grows
`3,256-2,583=+673`, and the fixture grows `1,864-1,610=+254`. These exact
component nets total `+1,848` before SSOT edits. Python under `src/rabbit`
remains `55,860`, so active implementation source is `71,801`, or `3,533`
above the PORT-00 baseline `68,268`. No Python/JAX source was deflated.

```text
added_lines: UNAVAILABLE (no clean all-surface stage boundary; exact component net +1848)
deleted_lines: UNAVAILABLE (no clean all-surface stage boundary; exact component net +1848)
net_lines: UNAVAILABLE for all authored surfaces; exact component net +1848 before SSOT
files_touched: 10 principal surfaces (2 Rust, fixture, consolidated replay, 6 SSOT docs)
token_use_exact: UNAVAILABLE
token_use_basis: the harness exposes no reliable F-08N-scoped exact counter
runtime_behavior_changed: yes
physics_behavior_changed: yes
known_blocker_reduced: yes
blocker_movement_ratio: 0.75
validation_strengthened: yes
cost_effectiveness_verdict: ACCEPT_WITH_LIMITS
generated_lockfile_lines: +0/-0/net 0
cumulative_rust_source_lines: 15941
superseded_python_jax_deletions: 0 in F-08N
endpoint_progress: MEASURED
```

The verdict is `ACCEPT_WITH_LIMITS`: F-08N moves a named network and measured
endpoint blocker, but adds source without deflation, propagates no nuclear-rate
uncertainty, and does not resolve the F-08D strict standalone authority RED.

F-09A moves the first electron-collision endpoint-consumption blocker without
adding a public dispatch or a standalone readiness/manifest/hash/figure surface.
Rust source grows `17,406-15,941=+1,465`; Python under `src/rabbit` remains
`55,860`, so active implementation source is `73,266`, or `4,998` above the
PORT-00 baseline `68,268`. The three corrected report sections add `+30/-24`,
net `+6`, before SSOT edits. No Python/JAX source was deflated.

```text
added_lines: UNAVAILABLE (no clean all-surface F-09A boundary; exact Rust-source net +1465)
deleted_lines: UNAVAILABLE (no clean all-surface F-09A boundary; exact Rust-source net +1465)
net_lines: UNAVAILABLE for all authored surfaces; exact Rust-source net +1465 plus report net +6 before SSOT
files_touched: 15 principal surfaces (6 Rust, 3 report sections, 6 existing SSOT documents)
token_use_exact: UNAVAILABLE
token_use_basis: the harness exposes no reliable F-09A-scoped exact counter
runtime_behavior_changed: no for non-test/public runtime; crate-private executable test endpoint added
physics_behavior_changed: yes, within the massless-MB thermal-moment model
known_blocker_reduced: yes
blocker_movement_ratio: 0.60
validation_strengthened: yes
cost_effectiveness_verdict: ACCEPT_WITH_LIMITS
generated_lockfile_lines: +0/-0/net 0 in F-09A
cumulative_rust_source_lines: 17406
superseded_python_jax_deletions: 0 in F-09A
endpoint_progress: MEASURED_PRIVATE_TEST_ENDPOINT
```

The `>400`-line exception is limited to the first collision source, its
equal-and-opposite FLRW consumer, and the weak/background/selected-31 vertical
slice that reaches the cold endpoint. The verdict remains
`ACCEPT_WITH_LIMITS`: a hard internal endpoint blocker moved, but source grew,
the path is not runtime-reachable, and finite-mass/FD normalization plus an
independent QED-on full-endpoint comparison remain open.

F-09B closes that finite-mass/FD/Pauli normalization and endpoint-consumption
blocker at `18,449` Rust lines, net `+1,043` from F-09A. F-10A then adds the
collisionless grid and consolidates shared quadrature at `+553/-78`, net
`+475`, reaching `18,924` Rust lines. Their cost verdicts remain
`ACCEPT_WITH_LIMITS`; neither adds a public caller or precision authority.

F-10B0 directly moves the measured thermal endpoint wall without a table,
surrogate, readiness gate, policy knob, or public dispatch. Rust source grows
from the uncommitted F-10A stage total `18,924` to `19,006`, net `+82`.
Python under `src/rabbit` remains `55,860`, so active implementation source is
`74,866`, or `6,598` above PORT-00.

```text
added_lines: UNAVAILABLE (the uncommitted F-10A boundary has no frozen blob)
deleted_lines: UNAVAILABLE (the uncommitted F-10A boundary has no frozen blob)
net_lines: exact Rust-source net +82
files_touched: 1 Rust source plus existing SSOT/report surfaces
token_use_exact: UNAVAILABLE
token_use_basis: the harness exposes no reliable F-10B0-scoped counter
runtime_behavior_changed: no for non-test/public runtime; crate-private endpoint rule changed
physics_behavior_changed: yes, bounded quadrature reduction within the same thermal model
known_blocker_reduced: yes
blocker_movement_ratio: 1.0 for the named pre-node endpoint-wall prerequisite
validation_strengthened: yes
cost_effectiveness_verdict: ACCEPT_WITH_LIMITS
generated_lockfile_lines: +0/-0/net 0 in F-10B0
cumulative_rust_source_lines: 19006
superseded_python_jax_deletions: 0 in F-10B0
endpoint_progress: MEASURED_PRIVATE_TEST_ENDPOINT
```

F-10B1 implements the first finite-mass HM electron/positron spectral action on
the F-10A comoving grid. Conservative elastic and pair event fluxes lock the
quadrature number and lepton-number moments; the analytic occupation response
is consumed in the FLRW Jacobian and the electron action's energy moment is
debited from the EM bath. This is a private coarse-grid endpoint, not a
precision or runtime promotion.

```text
added_lines: UNAVAILABLE (the uncommitted F-10B0 boundary has no frozen blob)
deleted_lines: UNAVAILABLE (the uncommitted F-10B0 boundary has no frozen blob)
net_lines: exact Rust-source net +1194
files_touched: 3 Rust sources plus existing SSOT/report surfaces
token_use_exact: UNAVAILABLE
token_use_basis: the harness exposes no reliable F-10B1-scoped exact counter
runtime_behavior_changed: no for non-test/public runtime; crate-private spectral endpoint added
physics_behavior_changed: yes, within the coarse tree-level electron spectral model
known_blocker_reduced: yes
blocker_movement_ratio: 0.75
validation_strengthened: yes
cost_effectiveness_verdict: ACCEPT_WITH_LIMITS
generated_lockfile_lines: +0/-0/net 0 in F-10B1
cumulative_rust_source_lines: 20200
superseded_python_jax_deletions: 0 in F-10B1
endpoint_progress: MEASURED_PRIVATE_COARSE_TEST_ENDPOINT
```

The above-400-line exception is limited to the first per-node event action,
its analytic occupation Jacobian and conservation projection, the coupled
FLRW energy debit, and focused solver/conservation tests. The order-six action
moments remain about `9.5%` below the independent thermal anchor. Order 32/24
recovers that anchor to about `0.17%`, but an order-eight/six endpoint exceeded
the frozen BDF/Rodas nodal relative ceiling. The direct support-boundary
explicit-`N` derivative also has a measured `~1.1e-4` step sensitivity. The
verdict therefore remains `ACCEPT_WITH_LIMITS`; F-10 stays active and F-11
stays parked.

F-10B2 replaces only the private spectral ODE coordinate and resolution
selection. The direct-occupation grid-12 Rodas RED was not hidden or repaired
with a wider tolerance: the state is now logit occupation, its Jacobian uses
the exact chain rule, and logistic underflow fails raw. The retained grid-16,
event-six/four rule is consumed by both solvers; a grid-20 BDF-only refinement
and an explicit-`N` support step ladder bound the same-code remainder. No RTA,
surrogate/table, policy/readiness surface, public caller, or lockfile change is
introduced.

```text
added_lines: UNAVAILABLE (the uncommitted F-10B1 boundary has no frozen blob)
deleted_lines: UNAVAILABLE (the uncommitted F-10B1 boundary has no frozen blob)
net_lines: exact Rust-source net +258
files_touched: 2 Rust sources plus existing SSOT/report surfaces
token_use_exact: UNAVAILABLE
token_use_basis: the harness exposes no reliable F-10B2-scoped exact counter
runtime_behavior_changed: no for non-test/public runtime; crate-private spectral coordinate/rule changed
physics_behavior_changed: no formula change; numerical state coordinate and selected resolution changed
known_blocker_reduced: yes
blocker_movement_ratio: 0.75
validation_strengthened: yes
cost_effectiveness_verdict: ACCEPT_WITH_LIMITS
generated_lockfile_lines: +0/-0/net 0 in F-10B2
cumulative_rust_source_lines: 20458
superseded_python_jax_deletions: 0 in F-10B2
endpoint_progress: MEASURED_PRIVATE_RESOLUTION_SELECTED_TEST_ENDPOINT
```

The selected BDF/Rodas endpoint takes roughly 173 seconds in its focused run,
so this is not a repeated-run performance promotion. Grid-20 Rodas and a
structurally independent full spectral endpoint were not run. The grid-20
BDF constants and the roughly 0.117 resolved-tail distortion are regression
readouts only. The verdict stays `ACCEPT_WITH_LIMITS`; only F-10C classical
diagonal neutrino--neutrino work opens.

At the historical F-10C0 boundary, the programme added the first primary-normalized same-flavour neutrino self-collision
component and consumes it in the existing private endpoint. The 8/8 focused
component gate passes. The production-to-tagged-target weak-action residuals
at radial orders 24/32/40/48/64 are
`4.26067%/7.08402%/4.82719%/3.53719%/2.07348%`; the selected GL16 linear
action remains about `7.54%` low. BDF/Rodas endpoint regressions execute at
`(N,t,N_eff)=(7.936729279099723,52680.93842348345 s,3.033105958925452)`
and `(7.936745110163169,52681.52513623983 s,3.032937181617912)`. The final
F-10C0 gate passes `217/217` release tests in `260.70 s` plus 0 doctests;
strict release all-target Clippy and formatting also pass.

```text
cost_line_scope: INTERIM_WORKING_TREE_NOT_A_PR; freeze a boundary before PR accounting
added_lines: UNAVAILABLE (the uncommitted F-10B2 boundary has no frozen blob)
deleted_lines: UNAVAILABLE (the uncommitted F-10B2 boundary has no frozen blob)
net_lines: UNAVAILABLE for all authored surfaces; exact Rust-source net +925 at the interim snapshot
files_touched: 11 (3 Rust sources, 6 existing SSOT files, 2 report sections)
token_use_exact: UNAVAILABLE
token_use_basis: the harness exposes no reliable F-10C0-scoped exact counter
runtime_behavior_changed: no for non-test/public runtime; crate-private spectral endpoint changed
physics_behavior_changed: yes, one same-flavour classical self-collision component added
known_blocker_reduced: yes, component normalization/conservation and endpoint consumption
blocker_movement_ratio: 0.50
validation_strengthened: yes
cost_effectiveness_verdict: ACCEPT_WITH_LIMITS
generated_lockfile_lines: +0/-0/net 0 in F-10C0
cumulative_rust_source_lines: 21383
superseded_python_jax_deletions: 0 in F-10C0
endpoint_progress: MEASURED_PRIVATE_COMPONENT_ENDPOINT; RESOLUTION_RED
```

Because this is an uncommitted continuation rather than a PR with a frozen
F-10B2 blob, the unavailable all-surface line split is an explicit interim
accounting limitation, not a claim of complete PR cost-line compliance.

The `>400`-line interim exception covers the CM kinematics and normalization,
four-leg conservative deposition, stable Pauli/logit response, analytic
Jacobian, endpoint integration, and focused physical falsifiers. It is not a
stage-close verdict: row 3 remains partial and seven folded channel rows remain
wholly absent but `SPECIFIED`, while selected
radial convergence remains RED despite the green 217-test release/static gate.

F-10C1 closes that declared radial-representation RED with limits. The retained
nonnegative linear basis now uses the fixed positive N48 map
`y=-3 ln(1-t)`. Its direct N32/40/48/64 residuals decrease through
`1.873525%/1.227393%/0.852441%/0.489704%`; two independent N128 auxiliary
integrations give a worst residual of `1.97715455697%` across five declared
smooth nonthermal profiles, below the frozen `2.1%` ceiling. FD moments,
electron/heavy thermal anchors, conservation, entropy, analytic Jacobians, the
combined first law, and explicit-`N` support checks pass. Composite N32 and
exponential N32/N40 remain honest RED candidates. This validates only the
declared profile envelope, not arbitrary distributions, continuum truth, a
complete catalogue, or a precision endpoint.

The matched pre-cache N48 cold endpoint took `2181.07 s` with maximum RSS
`51,020 KB`. A prepared self-event-geometry candidate took `1980.52 s`, a
`9.195%` reduction below the policy's about-`10%` partial-blocker threshold,
and raised maximum RSS by `3.277%`. BDF remained bitwise equal, while Rodas
followed a different floating-point path inside the frozen solver envelope.
The candidate's `112` Rust lines and cache-only delegation test were removed.
This negative result is retained as `DEPRECATED` evidence and is not endpoint
promotion.

The final retained no-cache tree repeats both BDF and Rodas states bitwise.
It takes `2191.66 s` with maximum RSS `50,440 KB`, or `+0.486%` wall and
`-1.137%` RSS relative to the initial no-cache baseline. The electron values-
only allocation cleanup is therefore an arithmetic-neutral simplification,
not performance progress. The measured cold wall is unchanged.

```text
cost_line_scope: INTERIM_WORKING_TREE_NOT_A_PR; freeze a boundary before PR accounting
added_lines: UNAVAILABLE (the uncommitted F-10C0 boundary has no frozen blob)
deleted_lines: UNAVAILABLE (the uncommitted F-10C0 boundary has no frozen blob)
net_lines: UNAVAILABLE for all authored surfaces; exact Rust-source net +386 from F-10C0
files_touched: 12 (4 Rust sources, 6 existing SSOT/handoff files, 2 report sections)
token_use_exact: UNAVAILABLE
token_use_basis: the harness exposes no reliable F-10C1-scoped exact counter
runtime_behavior_changed: yes; crate-private selected grid and allocation behaviour changed, with no public dispatch
physics_behavior_changed: no; collision formulas/conventions are unchanged, while the numerical representation/readout changed
known_blocker_reduced: yes, the declared radial-representation blocker closed with limits; cold wall remains
blocker_movement_ratio: 0.50
validation_strengthened: yes
cost_effectiveness_verdict: ACCEPT_WITH_LIMITS
generated_lockfile_lines: +0/-0/net 0 in F-10C1
cumulative_rust_source_lines: 21769
superseded_python_jax_deletions: 0 in F-10C1
endpoint_progress: MEASURED_PRIVATE_N48_ENDPOINT; WALL_UNCHANGED; REPEATED_RUN_PERFORMANCE_BLOCKER_REMAINS
```

Python source remains `55,860` lines, so active source is `77,629`, or `9,361`
above PORT-00. The exact Rust net is inside the 400-line warning boundary only
after removing the sub-threshold cache. The warning-level growth is accepted
with limits because it closes the declared N48 radial envelope with durable
falsifiers and reuses existing solver/report/ledger surfaces; it does not close
F-10C.

## Next action

D-044 closed the external open-breakthrough intake as `PROVENANCE_FAIL`
(`DO_NOT_REOPEN` for those exact bytes); the external prompt chain is
consumed and must not be re-dispatched. D-045 records the 2026-07-27 owner
grants: handoff durability (executed), one validation-only
`G-F10C1-REGRESSION` bundle re-run, and **OWNER-A** for exactly one
prospectively sealed, repository-free, nonphysical `C-R6-ORBIT-CHART`
discriminator with local sealed execution. OWNER-B (target replay) and
OWNER-C (`_independent_noqke.py` modification) are defined in D-045 and are
not granted.

Both granted actions are complete. The regression bundle ran in full and
FAILED at clippy only (`gk_recurse` too-many-arguments,
`src/isotropic_boltzmann.rs:2513`); `G-F10C1-REGRESSION` stays FAIL on
fresh evidence and its fix needs a separate owner decision. The D-046
OWNER-A discriminator passed adjudication:
`C-R6-ORBIT-CHART-MICROCASE=VALIDATED` (manufactured nonphysical microcase
only) and the sealed run's terminal line requests OWNER-B authorization.

D-049 through D-051 are complete: `G-F10C1-REGRESSION` is PASS on the
lint-fixed tree, and the live comparator carries the validated row-6
closure with all frozen static gates passing on the accepted bytes
(`STATIC_GATES_PASS_ON_CLOSED_SOURCE`). The open owner decisions are now:
(1) whether to authorize the **covariance-metrology program** — the
weak/mass-weighted identity plus the prospectively frozen `B_native`
bound (summation, basis, mass-solve, conditioning, interval, denominator,
state-envelope terms) that `G-F10-COVARIANCE-METROLOGY` requires;
(2) whether to grant **trajectory/endpoint eligibility** on the accepted
static bytes (the module ceiling makes this a separate decision, newly
live now that the static gates pass) — this is the route toward
`G-F10-INDEPENDENT-FLRW`; (3) whether to validate and close **row 9**
orientation members. Absent those grants, STOP/PRESERVE controls.
Exact-byte
remediation readiness, W7 execution, B3 implementation, and
independent-FLRW closure remain **FAIL**; preserve every exact failure,
bounded pass, unfinished D-037 axis, legacy bundle, user-owned review, and
raw `.singularhistory` evidence.

F-11 Bianchi/Type-I remains owner-paused. QKE/coherence, public dispatch, and
public production remain `FORBIDDEN`.

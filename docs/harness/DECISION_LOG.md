# Decision Log

| Date | Decision | Rationale | Alternatives rejected | Consequences |
|---|---|---|---|---|
| 2026-07-28 | Record D-052/D-053: the owner grants all three open programs (row-9 closure, covariance-metrology, trajectory/endpoint), sequenced; stage 1 closes as an adjudicated PASS — all pair conversions now carry both ordered orientation members at `K_t`/`16.0` (catalogue 27, fingerprint `(2,1,6,2,2,2,4,4,4)`), accepted as the final static catalogue `760a7c04` at `94d0f00`. e/mu self residual closed to `3.53e-11`; mu/tau regression `2.45e-11`; CP improved to `3.14e-11`. | D-046/D-048 validated the mechanism generically and on live row 6; rows 6/9 are structurally identical single-ordered pair conversions; the E3 discriminator directly measured the row-9 artifact and its removal. Run `run-20260728-f10-d053-row9-closure`, five envelopes 5/5. | Extending closure without a discriminating check; leaving the special-case branch; digit-level agreement gating. | Stages 2 (metrology bound deciding `G-F10-COVARIANCE-METROLOGY`) and 3 (independent trajectory deciding `G-F10-INDEPENDENT-FLRW`) bind bytes `760a7c04`. No gate moved at stage 1. |
| 2026-07-27 | Record D-051: close the OWNER-C implementation as an adjudicated **PASS** and accept the bytes — `_independent_noqke.py` sha256 `29a652fd` at `d22d001` carries the row-6 two-ordered-member closure. All frozen checks pass: 3/3 focused tests including the D-028 executing-gate assertion; GL48 `mu_tau_residual = 2.6019033208963758e-11` (18x under the `1e-10` cap, same-noise-class agreement 0.330 with the D-048 prediction); conservation `2e-16`, first law `0.0`, CP `4.59e-11`, entropy `>= 0`, equilibrium null in the recorded S0 class. | Run `run-20260727-f10-d050-ownerc-closure`: five envelopes, 5/5 unique findings, adjudication `3aa9b7d2`; freeze chronology verified at second resolution; three-hunk module diff only. | Flipping `G-F10-COVARIANCE-METROLOGY` on a single sub-cap cell (its identity + `B_native` program remains unbuilt); reopening the historical D-028 record for bytes `535370c1`; treating static-gate passage as trajectory/endpoint authority; closing row 9 without its own validation. | Terminal `STATIC_GATES_PASS_ON_CLOSED_SOURCE`. Trajectory/endpoint eligibility is newly live as a question but requires a separate explicit owner decision. Limitations recorded: same-model reviews, single execution per check, CP margin halved on this draw, same-noise-class (not digit-level) agreement, W5/D-047 scripts non-rerunnable on new bytes. |
| 2026-07-27 | Record D-050: the owner grants **OWNER-C** with a prospectively frozen implementation contract: carry the D-046/D-048-validated row-6 orientation closure in `_independent_noqke.py` (both ordered members at `16.0`, catalogue 24 -> 25, fingerprint row 6 1 -> 2), row 9 untouched pending its own validation, evaluator and authority ceiling unchanged, acceptance checks C1..C5 frozen before output. | D-048 established the mechanism and the closure's effect at the frozen cell. Blind pre-freeze review fixed one deterministic harness blocker (one-sided aggregate reconstruction) and one unmeetable digit-level agreement band before the freeze commit `d22d001`. | Editing the evaluator loop; closing row 9; digit-level agreement gating on float reassociation; unfrozen thresholds. | Acceptance decided solely by C1..C5; no gate movement from the grant; GL64/Radau/W7/B3/unblinding/F-11/QKE/public remain closed. |
| 2026-07-27 | Record D-049: the owner grants a validation-only lint-fix authority for `gk_recurse` and the full bundle re-run. The minimal `#[allow(clippy::too_many_arguments)]` attribute (commit `1697e76`, `cfg(test)` helper, zero codegen change) fixed the sole D-045 bundle failure; the full bundle then passed (fmt/check/clippy clean, 238/0 release tests in 880 s, 54-page report clean with page inspection) and `G-F10C1-REGRESSION` flipped fail -> pass. | Fresh D-045 evidence isolated the failure to a test-only lint on a naturally 10-argument recursion; the attribute is the smallest behavior-preserving fix and clippy's own suggested remedy. | Refactoring the recursion into a parameter struct (relabels the same values, larger diff); leaving the gate FAIL on a lint; skipping any bundle leg. | Gate flipped only on the all-command PASS (run `run-20260727-f10-d049-regression-fix`). The three scientific FAIL gates were untouched at that point. |
| 2026-07-27 | Record D-048: close the OWNER-B target replay as an adjudicated **PASS** and upgrade `D046-r6-orbit-chart-rabbit-applicability` to **VALIDATED**, bounded to: the D-028 native mu-tau covariance residual `4.666064056497196e-10` at the frozen GL48-Y24/10 MeV state is the row-6 single-ordered-member orientation artifact amplified by the `1/y^2` native map, removed by the D-046 derived-quotient closure to `3.8807636157303184e-11` (under the `1e-10` cap). | Run `run-20260727-f10-d047-ownerb-target-replay`: T-A reproduced the recorded residual digit-for-digit; live-code identity bitwise zero on both frozen states; internal row-6 localization `4.18e-11`; single-member artifact order one; conservation `2e-16`; freeze chronology verified against commit `9c99ecf`; adjudication `b2912459`, merged `110a00e4`. | Claiming a metrology bound, a general-state proof, or any trajectory/endpoint/production result; reopening or amending the D-028 record; moving `G-F10-COVARIANCE-METROLOGY` on this evidence; treating the terminal request as an OWNER-C grant. | Terminal line `REQUEST_OWNER_C_CONSIDERATION` is a request only; `OWNER-C=REQUIRED_NOT_GRANTED` for any `_independent_noqke.py` modification. The D-028 FAIL for its exact bytes stands. All gates and hard stops unchanged. Recorded limitations: same-model reviews, single deterministic execution, T-B bitwise-false at `1.01e-14`, undecomposed sub-cap floor, W5 drift provenance, adapted asymmetric identity state. |
| 2026-07-27 | Record D-047: the owner grants **OWNER-B** — one read-only target replay of the D-046-validated closure construction against the live `_independent_noqke.py` row-6 evaluator, under a prospectively frozen contract and script (freeze commit `9c99ecf`). The replay evaluates the absent reverse ordered member through an external mirror of the event loop; the module is imported unmodified. | Owner grant following the D-046 PASS terminal request. At the frozen D-028 state the mu/tau logits are bitwise identical, so the recorded residual is a discretization artifact; the D-046 lab predicts exact closed-object cancellation on symmetric states. Blind pre-freeze review: no blockers. | Editing `_independent_noqke.py` to run the reverse member (OWNER-C territory); reopening D-028 (its prohibition on orientation averaging binds rescue of that verdict, not this separately frozen owner-granted experiment); unfrozen thresholds. | Gated checks T-A..T-H frozen before first output; all-pass upgrades only the bounded applicability claim and may request OWNER-C consideration; no gate movement regardless of outcome. |
| 2026-07-27 | Record D-046: close the OWNER-A discriminator as an adjudicated **PASS**. The prospectively sealed, repository-free, nonphysical `C-R6-ORBIT-CHART` lab (seal `4c05ce2`, run `run-20260727-f10-ownera-r6-orbit-chart-lab`) passes the full acceptance battery, kills 10/10 semantic mutants for preregistered reasons, and replays byte-identically three times under network-off isolation. `C-R6-ORBIT-CHART-MICROCASE=VALIDATED` (manufactured nonphysical microcase only); lab code `IMPLEMENTED`; RABBIT row-6 applicability remains `PROPOSED`. | Six schema-v2 envelopes, 9/9 exact-unique findings, adjudication `569f7d6e`, merged `f7a74b26`. The closed two-member object is equivariant exactly and bitwise while the single member fails exactly; Reynolds one-half idempotence, retained antisymmetric response, exact conservation/entropy certificates, and the 1/12-vs-5/144 slot-identity witness all hold. | Declaring the microcase result a validation of collision physics, normalization, or any F-10 gate; granting OWNER-B implicitly from the terminal request line; skipping the recorded limitations (same-model blind reviews, pre-seal static fix loop, probabilistic MUT-5 kill, single-host determinism). | Terminal line `REQUEST_OWNER_B_AUTHORIZATION_FOR_TARGET_REPLAY_REVIEW` is a request only; `OWNER-B=REQUIRED_NOT_GRANTED`, and a passing replay would still require OWNER-C for any `_independent_noqke.py` change. Every scientific gate is unchanged; all D-045 hard stops remain in force. |
| 2026-07-27 | Record D-045: the owner grants (1) handoff durability for D-044, (2) one validation-only re-run of the full `G-F10C1-REGRESSION` command bundle on the current tree, and (3) **OWNER-A** for exactly one new prospectively sealed, repository-free, nonphysical `C-R6-ORBIT-CHART` discriminator with local sealed execution; define OWNER-B (target replay against live `_independent_noqke.py` row 6) and OWNER-C (`_independent_noqke.py` modification) as separate future owner decisions, neither granted. | Owner selections in the 2026-07-27 blocker-resolution session responding to the BD622 F-10 STOP/PRESERVE handoff; OWNER-B/C previously existed only in the D-044 exclusion clause without definitions; commit-plus-push durability policy chosen. | Leaving the regression gate FAIL on stale evidence; dispatching a new external auditor instead of a local sealed run; granting OWNER-B/C together with OWNER-A; designing the discriminator without a prospective seal. | The grants themselves move no scientific gate. `G-F10C1-REGRESSION` may flip only on an all-command PASS of the exact bundle. The OWNER-A discriminator must satisfy the D-044 sign/chart/quotient/JVP/mutant/replay obligations and the applicable D-042R gates adapted for local sealed execution; its PASS may only request OWNER-B authorization. W7, B3, T01--T12, Rust/JAX forward work, Radau, trajectory, endpoint, unblinding, F-11/Bianchi, QKE, and public work remain closed. |
| 2026-07-24 | Close the D-044 read-only intake of `F10_OPEN_BREAKTHROUGH_LAB_ab95ff52.zip` as `PROVENANCE_FAIL` at `XG-00-PROVENANCE`; set `PR_DAG=UNREACHABLE` and `DO_NOT_REOPEN` for these exact bytes; retain the narrower orientation-closed `C-R6-ORBIT-CHART` as `PROPOSED` with `OWNER-A=REQUIRED_NOT_GRANTED`. | Registered run `run-20260724-f10-open-breakthrough-intake` produced 49/49 exact-unique findings across seven envelopes. The package lacks the prospective contract/chronology, repository-free single runner, one finalist, semantic mutants, clean replays, raw logs, full manifest, isolation, accounting, and terminal route required by D-042R, and it imports the live RABBIT comparator while emitting physical collision actions. | Admitting the package on ZIP transport integrity or partial provenance; accepting label-only OCAR (general spectra obey `D(Pf;z)=-D(f;Rz)`, so flavour relabeling alone cannot build a reverse orientation chart); repairing the returned bytes in place; granting OWNER-A implicitly at intake. | Archive integrity and row-6 localization are preserved as bounded transport/external evidence only. `C-R6-ORBIT-CHART=PROPOSED`; only an explicit owner decision may authorize a new prospectively sealed repository-free nonphysical discriminator, and even that does not authorize OWNER-B/C, `_independent_noqke.py`, W7, B3, T01--T12, Rust/JAX, Radau, trajectory, endpoint, unblinding, F-11/Bianchi, QKE, or public work. All scientific gate states are unchanged. |
| 2026-07-24 | Preserve the first D-042 `PROVENANCE_FAIL` and issue an in-place D-043 correction that makes a fresh external screen executable from published commits without laundering missing historical bytes. | All current refs, reflogs, 1,249 blobs, and the preserved legacy Git database were searched. Required `4f4da8f0` survives only as local unreachable blob `5d583891` in orphan trees, while `e3ea99a9` is absent. The old contract therefore asked a GitHub auditor for non-distributable evidence. | Reconstructing or committing the orphan snapshot; importing protected dirty-tree files; requiring the old context pack or `/tmp` CAS logs; weakening provenance for the new experiment; ending all new-candidate work merely because old raw evidence is unavailable. | The corrected request uses `CODE_HEAD=bede5a9`, `EVIDENCE_BASE=204d3fa`, and the commit containing the prompt. Old unavailable bytes impose a historical evidence ceiling only; all new contract/code/test/raw/manifest bytes remain fail-closed. Scientific gates and downstream owner gates do not move. |
| 2026-07-23 | Record D-042's revised external executable-breakthrough request as ready for verbatim issuance, without reopening the repository or scientific programme. | Four blind design reviews informed the draft; two hostile cross-reviews rejected superseded hash `bab03230`; the corrected prompt `6b5545bd` passed replacement adjudication `9607e9cf` with 21 raw findings deduplicated to 20 keys. | The original three-prototype/19-file package; candidate-selected PASS definitions; hash/import-only mutation kills; repository deletion as a scientific gate; a mandatory PR list when no candidate passes; treating an external report as owner authority. | The external task may screen 6--10 ideas and run one sealed nonphysical prototype. `G-EXT-FEASIBILITY` remains FAIL, the current PR queue remains empty, and even an external PASS can only request a separate owner intake decision. |
| 2026-07-23 | Close D-041/EXT-01 as a fail-closed pre-output feasibility **FAIL** and archive the three registered candidates. | Five blind axes, two cross-steelman reviews, and final adjudication `18d6449d` leave zero candidate-wide passes. The same-space no-go is formalized, but the Petrov, analytic, and moving-remap families are not universally disproved. | Ranking non-passing candidates; treating local determinant, scalar-tail, or eventwise-entropy passes as whole-method authority; starting PR-01; silently changing a domain, norm, mesh, cap, or tolerance. | STOP/PRESERVE remains binding. OWNER-1 is not reached; PR-01 and all downstream implementation/execution nodes remain closed pending a materially new external construction and explicit owner authority. |
| 2026-07-23 | Close D-040's read-only legacy-JAX/Bianchi/DSMC survey. Exclude conventional DSMC from future Bianchi primary, precision, covariance/derivative, and endpoint-authority lanes; retain at most a separately authorized bounded stochastic cross-check. Treat legacy JAX mechanisms as semantic-rederivation clues only. | Five registered audit axes and adjudication `d7d34e41` normalize 37 raw findings to 34 exact keys. The no-stress Type-I law gives monotone `Sigma^2` damping; coupled oscillatory shear is unproved and remains `SPECULATIVE`. Isotropic neutrino DSMC is a real counterexample to categorical non-convergence, but it removes the directional moments that the Bianchi problem must resolve. | Restoring retired JAX drivers; importing calibrated RTA/AP or modified-Jacobian branches; claiming that oscillation alone prevents Monte Carlo convergence; using DSMC as precision or independent closure authority; selecting a solver before deriving the response spectrum. | D-039 STOP/PRESERVE remains binding. The ranked R1 premise discriminator, R2 exact physics contract, and R3 bounded deterministic Rust-target prototype are `PROPOSED` only. No implementation or scientific execution authority follows. |
| 2026-07-23 | Record D-039: the owner selects STOP/PRESERVE after D-038 and requests only a read-only external breakthrough/remedy design audit. | D-037 retains two admitted whole-design FAILs and D-038 localizes the current executable-contract root without an economical repair. R2 prompt audit `898fcf02` rejected authority/provenance/completeness ambiguity; the corrected exact request passed R3 `a424e9c9`, including 30 hash checks and zero-unmapped/operation-level novelty/`0.00` achieved-BMR rules. | Automatically exercising D-038's optional final design cycle; another exact-byte patch; output-driven method search; treating an external proposal or its editorial verdict as design authority. | The optional cycle is frozen, not activated. The response can inform only `DO_NOT_REOPEN` versus one fresh design-only review. W7 and B3 retain their later separate evidence/decision ladders; no edit, execution, implementation, trajectory, endpoint, or public authority follows. |
| 2026-07-23 | Accept D-038 as a completed read-only historical/adversarial adjudication with a route verdict of FAIL and a default of STOP/PRESERVE. Treat current D-035/W6 executable-contract debt as root cause, while limiting history-wide architecture-first to a recurrence amplifier rather than a universal root cause. | Seven-stage mapper/blind-audit loops, two mutual steelmen, six debate rounds, and adjudication `c145f298` reduce 32 exact-unique findings to seven causal families. Distinct Fort/D-027/D-028/D-029 failures retain rational bounded-discovery value; current shell, basis, tail, denominator/A4, and trace omissions were binding at sealing. | Blaming harness/reroot for science; treating all failures as isolated typos; declaring B3 impossible; launching a larger implementation/test push; automatically starting another exact-byte packet. | Exact-byte remediation, W7, B3, and independent-FLRW readiness remain FAIL. Only a new owner decision may authorize one final design-only, existing-surface cycle with a finite obligation matrix and terminal archive on any non-PASS/new class. No execution or implementation authority follows. |
| 2026-07-23 | Record D-037 as a fail-closed pre-adjudication design FAIL and stop on the first admitted non-PASS envelopes, exactly as D-035 requires. | Shared CAS contract `d2780e2c` froze the exact bytes and conventions. B3 `bead9788` and W7 `f785c2d2` contain 11/11 unique findings: four bounded passes but seven binding shell, basis, tail, event/trace, A0--A4 and whole-design failures. Parent diff/status remained `cea5aa70`/`b1d0bc24`; `.singularhistory` stayed `5abbc842`. | Continuing Wolfram/Sage after the hard stop; spawning Lean/SymPy; manufacturing a merge/adjudicator verdict; promoting row-ii, normalization, nullspace or entropy subclaim passes; editing sealed bytes; starting W7/B3 execution. | Wolfram/Sage are interrupted without results, Lean/SymPy remain registered but unspawned, and no merge/adjudicator exists. Effective design verdict is FAIL without an adjudicator claim. W7 execution/static implementation, B3, T01--T12, Rust, Radau, trajectory, endpoint, unblinding, F-11, QKE and public runtime remain forbidden pending a new explicit owner decision. |
| 2026-07-23 | Accept D-036 only after the fresh live R3 canary on the final Stop-side assignment-contract hardening: keep `agent_type`/`runtime_agent_type` equal to the actual Codex event type and bind the logical `review_role` separately with exact role-file and result-template hashes. | Dedicated D-035 CAS launchers failed before Start on an unsupported forced model. The supported `default` runtime now carries `cas_sage_singular` only as a sealed review role. Result `1b2f2e23`, assignment `7b62b201`, context `a364e7b4`, static fixtures `6/6`, the blocked invalid Stop, accepted corrected result, `/tmp` tool confinement, and identical non-run hashes validate the current supported boundary. | Weakening Stop identity checks; labelling a default runtime as a dedicated CAS runtime; accepting free-form role paths or result schemas; editing preserved D-035 results; hiding the first D-036 registration hash mismatch, Sage cache-path RED, stale follow-up Start, or rejected spawn invocation. | `G-HARNESS-INTEGRITY` is PASS within the supported schema-v2 parent/Start/Stop boundary. A fresh registered D-035 design review may start, but no W7 numerical/static execution, B3 work, Rust, T01--T12, Radau, trajectory, endpoint, unblinding, F-11, QKE, or public runtime is authorized by this repair. |
| 2026-07-23 | Accept the owner's approval only for D-035 exact-byte correction and one fresh blind review; do not treat it as W7 execution or B3 implementation authority. | D-034 adjudication `95870216` names six bounded defects, and the raw `.singularhistory` side effect `5abbc842` exposes a separate tool-containment gap. Sealed correction bytes are W6 `9caaa445`, source `60fc9668`, vectors `e991d415`, and Sage role `812bce01`; source-only checks pass without W7 output. | Running W7, T01--T12, B3 collision/static output, Rust, Radau, trajectory, endpoint, or unblinding; deleting the raw side-effect evidence; adding a new wrapper/gate; self-certifying authority after a PASS. | Main may run seven registered blind assignments on only those bytes. Any non-PASS or unauthorized write stops. Even PASS only supports a later explicit owner decision for W7 execution. |
| 2026-07-22 | Record D-034 exact-byte adjudication as FAIL and downgrade effective harness integrity to FAIL. | B3 `39e9394a`, W7 `98a73fce`, Wolfram `91c45257`, Sage `d8e4b2c8`, Lean `f19f64a9`, SymPy `c9f65215`, merged `a172b68f`, and adjudication `95870216` retain 31 unique findings. Global normalization/`G_F`, finite-mass source semantics, basis/ladder, and A0--A4 fail. Sage/Singular also wrote undeclared repository-root `.singularhistory`. | Promoting bounded subclaim passes; relying on structural `validate_harness.py` alone; deleting or hiding the unauthorized artifact; allowing the prior R4 canary to mask a later result-only-write failure. | `G-HARNESS-INTEGRITY`, `G-F10-COVARIANCE-METROLOGY`, `G-F10-INDEPENDENT-FLRW`, and `G-F10C1-REGRESSION` remain FAIL. All numerical/static implementation authority remains closed. |
| 2026-07-22 | Accept the owner's approval only for D-033 exact-byte remediation and a fresh blind review; freeze D-034 with a stricter W7-before-B3 authority split. | Candidate W6 `e0103e29`, unexecuted W7 source `5e8842d9`, and vectors `5a16c903` specify the previously missing factor, branch, basis, ladder, state, environment, and metrology graph; source-only 24+15/vector identity checks pass without W7/B3 output. | Treating owner approval as W7 execution or B3 implementation authority; importing production code; reusing D-028's cap; skipping the fresh review; letting design PASS directly open T01--T12. | Start one fresh registered blind design/CAS run. Any non-PASS stops. Even PASS requires a new owner decision for W7 execution only; B3 remains separately gated. |
| 2026-07-22 | Reject the current B3-v2/W7 bytes at blind adjudication and require exact-byte remediation plus a fresh blind PASS before any owner implementation decision can be sufficient. | B3 `fc283d4f` and W7 `8d490da0` identify critical under-specification; four CAS envelopes retain exact subclaim evidence but leave all-channel normalization and required closures incomplete; normalized merge `1c28dcfb` has 28 unique findings and adjudication `e5b21e26` is overall FAIL. | Promoting the four-axis nullspace pass to whole-design authority; accepting three-axis row-ii/entropy agreement as full closure; treating generic shell roots as the unspecified B3 reduction; allowing owner authority alone to bypass failed review; hiding dedicated-agent platform failures. | D-033 binds. Only design/W7 source and test-vector remediation is in scope. W7 numerical execution/static implementation, B3 output, T01-T12, Rust, Radau, trajectory, endpoint, unblinding, F-11, QKE, and public runtime remain forbidden. |
| 2026-07-22 | Accept D-032's supported parent/SubagentStart/SubagentStop workaround after the fresh R4 live canary, and set `G-HARNESS-INTEGRITY` PASS only inside that boundary. | R4 `5a979857` verified the exact registered prompt header and assignment SHA, observed Start bootstrap/preflight PASS, proved the invalid first Stop is blocked and the corrected result accepted, and wrote only its unique result; parent pre/post non-run diff `828cd996` and status `7ad89840` are identical. | Treating VS Code project PreToolUse as restored; promoting static fixtures alone; weakening write attribution; converting harness readiness into scientific validation or implementation authority. | Registered blind B3-v2/W7/CAS design review may start. W7 numerical execution/static implementation, B3 collision output, Rust, T01--T12, Radau, trajectory, endpoint, RABBIT unblinding, F-11, QKE, and public runtime remain unauthorized. |
| 2026-07-22 | Supersede D-031's PreToolUse launch authority with a supported parent/SubagentStart/SubagentStop contract. Main validates registration/header and hashes the non-run tree; Start injects context/identity; the result records the prompt/Start contract and assignment SHA-256; Stop validates all of it on every completion attempt. | Reloaded R3 received no allow-rewrite attestation, and a separate registered spawn containing a reserved field was not blocked. Both agents nevertheless proved Start injection and retry-safe Stop enforcement. This is an observed VS Code collaboration boundary, not an advisory assumption. | Claiming PreToolUse ran because it was trusted; adding another wrapper/daemon; editing failed R3 evidence; weakening result identity or write attribution; treating static fixtures as gate closure. | Project PreToolUse is removed as a false authority surface. Four focused fixtures validate the replacement statically. `G-HARNESS-INTEGRITY` remains FAIL until a fresh R4 live canary validates the full parent/Start/Stop chain and result-only write boundary. |
| 2026-07-22 | Move registered spawn-header enforcement to `PreToolUse(Agent|spawn_agent)`, keep `SubagentStart` for canonical context/identity injection, and validate every `SubagentStop` retry against one shared RESULT_ENVELOPE contract. | Official Codex payloads expose the spawn message only at PreToolUse; SubagentStart exposes agent identity/type but not that message. R2 also proved that returning immediately on `stop_hook_active=true` admits an invalid retry. | Searching arbitrary SubagentStart scalars for the prompt; treating an advisory preflight as admission; skipping retry validation; rewriting either failed canary; closing the gate from fixtures alone. | The minimal existing hook scripts now implement the lifecycle at the correct events and six fixtures pass. VS Code must reload and trust the changed hook definition, then a fresh live canary must pass. Until then `G-HARNESS-INTEGRITY` remains FAIL and no W7/CAS/B3/Rust work is authorized. |
| 2026-07-22 | Preserve `f10-independent-validation-blocker` as audit evidence, start `f10-independent-validation-b3v2` from `main@ca3a013`, correct the B3-v2/W7/D-030 claim contract, and stop at the failed trusted-hook canary. | The old W6 weak form was species-blind, mixed finite-electron-mass and massless kinematics, overstated W3/W5, and lacked a method-specific cap. The replacement contract corrects those defects, but the real canary did not observe SubagentStart injection or automatic SubagentStop enforcement. | Merging `8eecb83`; deleting the pre-existing AD/Bianchi spec; treating static validator success as hook evidence; copying D-028's `1/y^2`/`~1.2e-8` model; continuing blind reviews or implementation without the stage-0 gate. | `G-HARNESS-INTEGRITY`, `G-F10-COVARIANCE-METROLOGY`, and `G-F10-INDEPENDENT-FLRW` remain **FAIL**. W6 is `PROPOSED`; W3 is ratio/reduction-only; W5 is localization-only; the AD/Bianchi spec is `PROPOSED/PARKED` and implementation is `FORBIDDEN`. No W7/CAS/B3/Rust output is authorized until project hooks are reviewed/trusted in a fresh session and a replacement canary passes. |
| 2026-07-19 | Re-root the publishable repository as a curated fresh `main` history while retaining an exact local legacy backup; exclude `out_of_scope/` from every public commit and quarantine generated/untracked bulk data. | The legacy repository had 1,336 HEAD commits, no remote, a roughly 1.0 GiB object store with 5,726 unreachable objects, and about 14 GiB of local generated/audit material. A portable bundle alone cannot preserve reflogs, unreachable objects, index state, or the dirty worktree. | In-place history filtering; committing then deleting `out_of_scope/`; deleting local evidence; publishing generated outputs; retaining unrelated packages in the local venv. | The active public branch starts from the curated working state. The legacy `.git`, verified all-ref bundle, dirty patches, untracked archive, `out_of_scope/`, raw harness runs, and quarantined artifacts remain under ignored local `.publish-local/20260719T052443Z/`. This repository-maintenance decision changes no physics, validation, solver, runtime-authority, or claim status. |
| 2026-07-12 | Use `TYPEI_AUGMENTED_NOQKE_UNIFIED_FUTURE_PLAN.md` as the sole publication-code specification and execute PUB-00 -> PUB/M/N/B DAG with independent role separation. | The independent audit found stale competing plans and claim promotion beyond current code and raw gates. Consolidation is the anti-drift exception that prevents more wrapper growth. | Continuing deleted AP/FB/BD sequences; adding a new publication-spec file; treating v1 table, ORACLE, or gold proximity as validation. | Old roadmaps and phase prompts become historical. Runtime claims remain RED until their named executable gates pass. |
| 2026-07-12 | Close PUB-01 revision 7 as code-path VALIDATED while preserving gold and physics as RED. | Independent review accepted the exact-shape inference and release-truth diff; a detached clean tree passed focused and production lanes, while the two pre-existing numerical gold failures reproduced exactly. | Relaxing the gold floor/envelope; xfail/skip quarantine; calling runtime regression or historical shear profiles publication validation; enabling non-traceable BBN JAX samplers. | PUB-02 becomes the active implementation PR. G-01 still depends on real gold closure, and B-03/B-05 still own finite-shear/inference claims. |
| 2026-07-12 | Close PUB-02 as PUB-02C `CLOSED_WITH_LIMITS` and abandon revision-4 autonomous-`dfdt` wiring. | The frozen numerical primitives passed their clean probes, but another 86-test JAX wiring slice consumed 662.39 seconds and did not move endpoint wall or publication physics. | Promoting JAX Rodas5P; completing revision 4; treating semantic callback reduction as endpoint performance. | BDF/SciPy remains number-of-record; PUB-02C evidence is solver-primitive only; gold remains RED. |
| 2026-07-12 | End JAX-first freezing and make Rust AOT the active implementation target while preserving existing scientific and algorithmic knowledge. | Current Python/JAX scale and runtime obstruct iterative novel-physics development; existing FLRW/collisionless anchors already provide the migration baseline. | Freezing the full Python/JAX physics stack first; discarding established algorithms; wholesale revival of the deleted Rust crate. | PORT-00 terminates JAX expansion; R-01 must deliver an endpoint-consumed Rust vertical slice; JAX becomes a bounded oracle and superseded runtime surface is later deflated. |
| 2026-07-13 | Close R-01 with limits as a private Rust Tier-1 LRS vertical slice, without changing number-of-record or physics authority. | The clean endpoint-consumed implementation preserved the frozen algorithms, met all parity gates, reduced the warm canonical endpoint median to `0.494929x`, held RSS at `1.0x`, and stayed within `501/197` native/Python integration LOC. | Another JAX optimization; a crate-only microbenchmark; line-by-line Python control-flow translation; authority promotion before collision integration and source deflation. | R-02 becomes next. SciPy/BDF remains number-of-record through R-06; zero substantive Python/JAX deflation in R-01 is accepted only as bounded migration debt, not as completion of the language transition. |
| 2026-07-13 | Reject the combined R-02, split coordinate/catalogue substrate from HM physics, and close R-02A with limits. | Formatter-bypass review exposed `633+4` normal-format lines; the readable combined remediation remained `505` lines. The split produced an honest `269/270`-line private R-02A with test-first RED, exact 18/9 topology, independent reviews, and clean validation. | Raising the 400-line gate after results; opaque macro/statement packing; weakening physical falsifiers; reviving the legacy separable kernel; continuing JAX development. | R-02B becomes the next base-locked PR and may only add separate HM files with a one-way dependency on immutable R-02A. `cp_average` remains structural until B, and R-03/R-06 retain operator/runtime authority. |
| 2026-07-13 | Close R-02B with limits at corrected technical candidate `cfc16e6d222af4c391d4a3a957d110662543078e`. | The clippy-clean test-only RED, physical recoil/pair reductions, CP/crossing negative controls, nonuniform-input event fold, strict LOC/ABI/write-set gates, independent reviews, and fresh detached validation all passed. The mixed-owner `0dfd1f8` candidate was rejected and reverted before evidence was replayed. | Accepting the mixed falsifier/production commit; promoting event algebra to absolute normalization or a collision operator; reopening JAX development; importing the legacy separable kernel. | R-03 becomes next. SciPy/BDF remains temporary number-of-record, R-03/R-04 own operator/normalization evidence, and R-06 retains runtime authority and Python/JAX deflation. |
| 2026-07-13 | Split R-03 into A/B/C and close R-03A with limits at `3150541f91d59266bd3a87db26f1201c58b20de4`. | A single direct-operator PR would repeat the rejected combined R-02. Three pre-code review cycles repaired the event spec; frozen signed/raw/FD/topology falsifiers, a sole-error RED, bounded `336/350` Rust lines, independent reviews, and detached validation then passed. | Freezing the full Python/JAX operator first; combining occupation algebra, matrices, kinematics, quadrature, and normalization; unsigned weight tests; promoting pointwise consistency to operator validation. | R-03B next owns supplied-event action/matrices/fold; R-03C later owns physical event generation/integration. No operator, normalization, runtime, BBN, Rust-authority, or public-production claim is promoted. |
| 2026-07-13 | Abandon R-03B1/R/S and close final replacement R-03B1T with limits at `4946064e42976cc848b2c63655d13ae338c9dcd9`. | The earlier locks were infeasible or falsified by source/provenance counterexamples. B1T preserved the frozen physics tests, bounded normal Rust at `432/445`, closed external-source/import escapes, passed independent physics/Rust review, and completed detached validation with inherited gold RED. | Raising the original LOC gate after results; accepting contradictory regex gates; external/symlink source loading; combining action with matrices/fold/physical integration; reopening JAX. | Immutable supplied-event and explicit-six nonlinear action algebra is IMPLEMENTED privately. R-03B2R owns true `J0`, B2B owns generalized response, B3 owns structural fold, and physical `L=2`, normalization, runtime, Rust authority, and public production remain unpromoted. |
| 2026-07-13 | Abandon R-03B2A after two remediation rounds and close replacement R-03B2R with limits at `1e2ce3ac5e08cab990bf07629f813ec73e6cc17e`. | B2A's first locks forced per-event `f6` scans or made RED wiring impossible; its final lexical gate still overclaimed proof of exclusive allocation/materialization. B2R assigned semantic proof to independent final-diff review, preserved a bounded `398/400` Rust slice, passed exact FD/canonical/overflow falsifiers, and completed a detached 19-command wave. | A third token-regex patch; accepting total-only matrices; independently assembling total physics; broadening tolerance; combining generalized response, fold, physical integration, runtime, or JAX work. | Private channel-resolved and canonical-total scalar-action `J0` is DERIVED/IMPLEMENTED only. R-03B2B next owns generalized response; physical `P2/L=2`, normalization, runtime, Rust authority, and public production remain unpromoted. |
| 2026-07-13 | Reject the initial/R1/R2 generalized-response locks, roll R2 back, and close R-03B2B-R3 with limits at `8cb79c5347efe322f3bbcd42222e3983c71c7ffa`. | The first falsifier exceeded its frozen LOC, R1 forbade the event-count access required by its own length-precedence test, and R2 final review found exact test-layout drift after cascade remediation. R3 preserved the algorithms but rebuilt spec -> sole-E0432 RED -> production ancestry, passed independent physics/Rust review, and completed the detached 19-command wave. | Raising the `105`-line falsifier ceiling; mapping invalid occupations to a length error; accepting behaviorally good code that violated its exact spec; reusing stale R2 artifacts; combining fold, physical `P2`, integration, runtime, or JAX work. | Private generalized target/coupled supplied-response algebra is DERIVED/IMPLEMENTED only. R-03B3 next owns structural `P A6 L`; physical `P2/L=2`, normalization, runtime, Rust authority, and public production remain unpromoted. |
| 2026-07-13 | Abandon R-03B3-R1, combined R-03C1, and original R-03C1A; close corrected R-03C1A-R1 with limits at `3d1b538e34a8d981e35ea656c5da415f5c45223e`. | The rejected slices failed their frozen protocol, LOC, or reachability experiments. R1 retained their useful algorithms, froze three independent point falsifiers, produced the sole-E0432 RED, closed a review-found radial-constructor bypass, and passed independent physics/numerics plus detached clean validation. | Reopening structural B3; widening falsifier LOC or tolerances; using formatting suppression; adding a support-only wrapper; combining point construction with quadrature; continuing Python/JAX development. | Private finite-mass supported-point scalar/P2 density is DERIVED/IMPLEMENTED and its named construction invariants are VALIDATED. R-03C1B next owns separately specified quadrature; integration, absolute normalization, runtime, endpoint/BBN, Rust authority, and public production remain unpromoted. |
| 2026-07-15 | Replace the collision-first future order with a bottom-up Rust BBN ladder and park R-03C1B until F-09. | The earliest reachable Git snapshot is already a mature Python/JAX system with 101,585 root-commit insertions, while current Rust collision work has no minimal Rust BBN endpoint consumer. Independent review also found EOS continuum, decoupling-entropy, Born-integral, and reaction-table identity blockers below the collision layer. | Continuing R-03C1B next; translating the remaining Python/JAX stack wholesale; treating R-01 parity as an independently validated FLRW foundation; discarding accepted R-02/R-03 work. | F-01 dual solver, F-02 tree-level FLRW, F-03 Born weak, F-04 named 12-reaction network, and F-05 coupled endpoint become the critical path. Historical collision implementations retain their exact claim ceilings and re-enter only after matched standard anchors. |
| 2026-07-15 | Close F-01 and F-02 with limits, then open F-03 Born weak development. | The dual solver passed exact/stiff/Robertson/event/failure/determinism gates, while the ideal FLRW model passed independent continuum EOS, low/high limits, explicit two-leg entropy conservation, Friedmann units, and dual-solver endpoint checks. A review-found branch-straddling Jacobian was fixed by stopping and restarting at `T_dec`. | Treating the old GL64/QED Tier-1 path as continuum authority; integrating across the entropy discontinuity in one solver leg; promoting the ideal model to BBN or standard-`N_eff` authority. | F-03 may consume F-01/F-02 privately. SciPy/BDF remains number-of-record; F-04/F-05 and all later corrections/transport/shear gates remain open. |
| 2026-07-15 | Close F-03 with limits at the six-channel Born kernel and standalone neutron freeze-out, then open F-04. | Every named channel matches independent adaptive integrals, the analytic lifetime normalization removes the inherited GL64 bias, and an independent `T_gamma`-parameterized SciPy solve agrees with both Rust solvers through the explicit decoupling restart. | Calling standalone `X_n` a nuclear BBN endpoint; importing low-temperature rate floors or clipping; beginning QED, corrected weak rates, collisions, or anisotropy before the minimal network. | F-04 becomes the only active implementation slice. F-05 still waits for its named-table, conservation, Jacobian, manufactured-solution, and raw-failure gates. |
| 2026-07-15 | Close F-04 with limits at the isolated named minimal-network kernel and open F-05 coupling. | Both tables agree exactly only after the executable identity remap; all stoichiometry, density/symmetry powers, analytic Jacobian, conservation, boundary, raw-failure, and shared-kernel manufactured gates pass. Independent review found and then closed a mass-fraction-versus-molar manufactured-test gap. | Positional table use; calling shared-data Python parity independent rate validation; promoting an isolated kernel to final abundances; carrying a constant late-time eta through e+/e- entropy transfer without a density derivation. | F-05 becomes the only active slice and must evolve baryon density as `a^-3` or prove an equivalent instantaneous-eta convention before coupling. |
| 2026-07-15 | Close F-05 with limits at the explicit simplified coupled Rust endpoint and open F-06 matched-anchor/deflation work. | The coupled state evolves `ln n_b` exactly as `a^-3`, preserves zero nuclear handoff without seeds or clipping, passes both solvers plus refinement and raw-domain gates, and agrees with an independently formulated SciPy endpoint. The independent review found no BLOCKER/HIGH. | Calling the default activation plateau-converged; tuning seeds or clipping implicit stages; promoting simplified observables to precision or Rust authority; starting QED/corrections/collisions before matched anchors. | F-06 must match start condition, constants, rates, corrections, neutrino treatment, tolerances, and observables against PRIMAT plus another external implementation, then delete substantive superseded Python/JAX runtime surface. SciPy/BDF remains number-of-record. |
| 2026-07-15 | Close F-06 with limits, promote Rust only as the private number-of-record for the exact minimal matched profile, and open F-07. | Live PRIMAT blocks and matched-table endpoints, the declared shared-input LINX RHS/integrator ladder, Rust dual-solver/conservation/raw-failure gates, full endpoint timing, and source deflation all pass. Active source is `65,377` lines versus the reconstructed PORT-00 implementation baseline `68,268`. | Treating stock and custom PRIMAT results as interchangeable; calling LINX an independent background/nuclear-data validation; retaining callable JAX endpoint quarantine; extending minimal-model agreement to QED, corrected weak rates, full network, collisions, anisotropy, or public production. | Public dispatch is `auto`/`scipy` only and high-level JAX endpoint/runtime surface is deleted. Rust authority is private and profile-specific. F-07 finite-temperature QED is next; all later corrections/transport/shear remain parked. |
| 2026-07-15 | Close F-07 with limits at scalar finite-temperature QED and open F-08A zero-temperature CCR. | The exact zero branch is bitwise preserved; `dPa`, `dPb`, `dPe3`, pressure derivatives, thermodynamic identities, limits, and quadrature converge against independent formulations; and a live matched PRIMAT run validates the leading `dPa+dPe3` endpoint delta. PRIMAT endpoints omit `dPb`, and direct 48-by-48 exchange quadrature leaves a material endpoint cost. | Misclassifying `dPb` as `O(e^4)`; translating PRIMAT's incomplete two-file table as the complete convention; calling dual-solver agreement external complete-endpoint validation; beginning finite-mass, collisions, or transport before a corrected weak consumer exists. | The complete scalar endpoint stays IMPLEMENTED/internal-only and F-07 is `ACCEPT_WITH_LIMITS`. F-08 is split into CCR, finite-mass, weak-magnetism, and balance-restoring thermal blocks; only F-08A is active. |
| 2026-07-15 | Close F-08A with limits at zero-temperature CCR and open F-08B finite mass without weak magnetism. | The Born compatibility path is bitwise unchanged; PRIMAT component/lifetime anchors, all six physical rates, live matched C/Python endpoint deltas, dual Rust solvers, and an independent `T_gamma`-variable DOP853 standalone oracle all pass. The fresh-process oracle repeat is bitwise identical, and its tolerance and nested-QED-grid spreads remain below the recorded envelopes. | Translating the legacy scalar finite-mass multiplier; calling formula review a live LINX execution; using Rust/PRIMAT proximity to claim precision-standard/full-network authority; beginning weak magnetism, thermal weak terms, collisions, or transport before recoil has an endpoint consumer. | F-08A is `ACCEPT_WITH_LIMITS` for the exact matched profile only. F-08B becomes active and must implement additive finite-mass Fokker-Planck terms with anomalous weak magnetism explicitly disabled; F-08C-D and F-08N remain parked. |
| 2026-07-15 | Close F-08B with limits at additive first-order finite-nucleon-mass Fokker-Planck terms with `delta_kappa=0`, then open F-08C physical weak magnetism. | The direction-owned `m_p/m_e` and `m_n/m_e` channel algebra, no-WM lifetime correction, six rates, nonzero modified-detailed-balance residual, direction-specific quadrature remediation, independent `T_gamma`-variable standalone solve, dual Rust endpoints, and live matched PRIMAT Python endpoint pass; the external validator reports 17/17 and adversarial review reports no BLOCKER/HIGH/MEDIUM. | A scalar recoil multiplier; tuning the first-order detailed-balance residual to zero; leaking physical weak magnetism into F-08B; claiming stock/precision/C authority from a custom Python profile; moving directly to thermal weak, network, collision, transport, or QKE work. | F-08B is `VALIDATED / CLOSED_WITH_LIMITS` only for the exact matched no-WM profile. PRIMAT C remains SKIPPED. F-08C becomes active through the same coupling algebra; F-08D, F-08N, collisions/transport, LRS/non-LRS remain parked and QKE remains FORBIDDEN. |
| 2026-07-15 | Close F-08C with limits at physical anomalous weak magnetism and open F-08D complete balance-restoring finite-temperature weak corrections. | Rust activates `delta_kappa=3.70589007463` through one generalized F-08B/F-08C finite-mass evaluator. Component anchors, the nonzero `3.6755293490786656e-5` first-order detailed-balance residual, GL320 weak-only standalone solve, fresh exact two-repeat PRIMAT v0.3.2 Python/C matched endpoints, dual Rust BDF/Rodas endpoints, the 23/23 external validator, 5/5 coordinated mutation rejection, and final Rust 150/150 plus strict static gates pass. The initial `4.0784416011074853e-8` GL RED was closed by physical panel splitting to `9.259088912250135e-9` without widening the frozen ceiling. | A scalar weak-magnetism factor; altering the F-08B zero-coefficient branch; tuning the detailed-balance residual to zero; widening the pre-Rust `N=3e-7` or `X_n=6e-8` budgets; hiding the initial GL RED; claiming stock/precision/public authority; beginning F-08N, transport, collisions, anisotropy, or QKE. | F-08C is `VALIDATED / CLOSED_WITH_LIMITS` and `ACCEPT_WITH_LIMITS` only for the matched custom-table, first-order, ideal instantaneous-decoupling profile. Exact Rust growth is `+787`; active source is `69,324`, `1,056` above PORT-00, with zero Python/JAX deflation. F-08D becomes active; F-08N, collisions, transport, and Type-I stay parked, and QKE/public production remain FORBIDDEN. |
| 2026-07-15 | Close F-08D with limits at the complete four-subterm finite-temperature radiative correction and open F-08N network expansion. | Rust evaluates true-photon, differential-bremsstrahlung, `L1`, and `L2+3` terms as one signed directional block over F-08C. A first low-temperature tensor-GL run was honestly RED; physical `E=q` and `k=|E±sq|` panel boundaries closed it without changing formulas or frozen envelopes. Six pre-Rust complete-sum anchors, the resolved independent subterm decomposition, all 56 private-table intervals in both directions, the 10 MeV Jacobian, dual standalone and matched endpoints, bitwise repeat, external 25/25 validation, and 5/5 mutation rejection pass. | Widening direct/component/interpolation ceilings; tuning to C VEGAS knots; treating tiny SciPy subterms as relative precision anchors; hiding the strict standalone `X_n` RED; splitting the four terms into endpoint modes; beginning transport, collisions, anisotropy, or QKE before network expansion. | F-08D is `VALIDATED / CLOSED_WITH_LIMITS` only for the conditional matched 12-reaction profile. Strict standalone parity remains BLOCKED because direct Rust and the noisy C-knot/global-quadratic comparator differ by `O(1e-6)` in `X_n`; C global/local interpolation moves `Yp` by `1.0021545698846168e-6`. F-08N becomes active; F-09/Type-I remain parked and QKE/public production remain FORBIDDEN. |
| 2026-07-15 | Close F-08N with limits at Rabbit's selected 31-row AC2024 central-rate network and open F-09 electron-collision integration. | The identity/reference audit proves all 31 rows and 60-knot forward tables against source; Rust binds names rather than positions, consumes stored `alpha/beta/gamma` without reconstructing them from rounded Q, preserves the first 12 rows, and evolves Li6. Pre-Rust external budgets, PRIMAT exact-reverse/private-injection and staging diagnostics, Rust BDF/Rodas5P plus refined BDF endpoints, nested 31-minus-12 effects, baryon conservation, log-Jacobian checks, determinism, 46/46 external validation, 5/5 external mutations, stored replay, and 167/167 release tests pass. | Calling the custom subset a stock PRIMAT network; replacing stored gamma from raw Q; tuning rates or widening the two honest conservation REDs; claiming rate-uncertainty propagation or precision abundance authority; using endpoint agreement to erase F-08D strict RED; starting QKE or Type-I before collision/transport layers are consumed. | F-08N is `VALIDATED / CLOSED_WITH_LIMITS` for the exact central-rate, all-31-from-F06-handoff, instantaneous-decoupling profile. F-08D strict standalone precision remains BLOCKED. F-09 becomes the sole active implementation slice; R-03C1B may re-enter only as an endpoint-consumed F-09 sub-slice, while F-10/F-11 remain parked and QKE/public production remain FORBIDDEN. |
| 2026-07-15 | Accept F-09A with limits as the massless-MB thermal-moment baseline, but keep F-09 open for finite-mass FD/Pauli integration. | The analytic electron source satisfies formula, sign, scaling, multiplicity, null, entropy, and first-law checks and reaches a private selected-31 cold endpoint through BDF/Rodas5P. Adversarial review exposed an initially ineffective full-endpoint tolerance claim and missing coupled-Jacobian parity; the final implementation tightens every leg, lowers the absolute floor, narrows budgets by orders of magnitude, and adds a five-point check. The full QED-on endpoint still lacks an independent external solve and has no non-test caller. | Reusing the deprecated `C_RATE=210` RTA; calling dual-solver agreement independent abundance validation; promoting a unit-test endpoint to runtime authority; opening F-10 after only a massless classical moment; adding a standalone readiness/table gate. | F-09A is `IMPLEMENTED / VALIDATED` within the declared model and `ACCEPT_WITH_LIMITS`. F-09B must integrate and independently normalize the finite-mass HM FD/Pauli operator and replace the analytic source in the same private endpoint. F-10/F-11 stay parked; QKE/public production remain FORBIDDEN. |
| 2026-07-16 | Close F-09B with limits at the finite-vacuum-electron-mass FD/Pauli thermal moment and open only F-10 collisionless isotropic classical transport. | The independent plasma-frame/CM component and convergence ladder, direct HM construction oracle, massless-MB limit, null/sign/entropy/cold suppression, event energy and channel number/lepton topology, first-law closure, independent QED-off DOP853/Radau endpoint, Rust BDF/Rodas, tighter BDF, coupled Jacobian, conservation, and repeat pass. The source is consumed by the existing private selected-31 endpoint. | Calling order six continuum truth; fitting a suppression factor; reviving the deprecated RTA; treating a thermal `N_eff` readout as precision; opening spectral collisions, neutrino-neutrino transport, Type-I, QKE, or public runtime authority at once. | F-09 is `VALIDATED / CLOSED_WITH_LIMITS` inside the thermal tree-level model. F-10 begins bottom-up at collisionless isotropic redshifting. The measured `~123/544 s` BDF/Rodas full-leg cost remains a direct blocker before per-node collision consumption; no new readiness/table surface may substitute for moving it. |
| 2026-07-16 | Close F-10A with limits at the collisionless isotropic comoving-energy grid and keep F-10 open for collisions. | The `q=a p` convention makes the massless Liouville equation exactly `df/dN=0`; arbitrary occupations preserve comoving number and energy, FD quadrature converges, the distribution contributes to the Friedmann/time endpoint, analytic Jacobian/explicit-time derivatives pass five-point checks, both Rust solvers preserve every occupation bit, and an independent continuum-FD SciPy endpoint agrees. | A moving physical-momentum advection scheme before fixing coordinates; calling a zero-collision distribution a decoupling/distortion result; adding spectral electron or neutrino-neutrino actions in the same slice; opening Type-I/QKE/public runtime work. | F-10A is `VALIDATED / CLOSED_WITH_LIMITS`; F-10 remains `IN_PROGRESS`. F-10B must first move the measured F09B wall or validate a bounded direct-operator reduction, then add endpoint-consumed electron spectral collisions. |
| 2026-07-16 | Accept F-10B0 with limits as the direct thermal-endpoint wall reduction and keep spectral F-10B open. | Polar four stays below `2e-5` relative to polar six across six temperature pairs and both flavour couplings; the independent endpoint shifts thermal `N_eff` by only about `-9.1e-9`; BDF/Rodas cold legs improve `2.28x/2.16x`; the tighter BDF repeat and 198/198 release suite pass. Polar three was falsified at `4.69e-5/7.63e-5` and rejected. | A fitted or interpolated surrogate; accepting polar three; calling a component timing endpoint progress; claiming the collisionless F10A grid now has collisions; opening neutrino-neutrino, Type-I, QKE, or public runtime work. | The named pre-node performance prerequisite is `VALIDATED / CLOSED_WITH_LIMITS`, but F-10B remains `IN_PROGRESS`. Next derive, validate, and consume the electron spectral action on the existing comoving grid; no table/readiness/policy surface may substitute. |
| 2026-07-16 | Close F-10B1 with limits at the coarse conservative electron spectral action, but keep F-10B active at resolution and support smoothness. | The direct finite-mass HM event action has an analytic common-FD null, conservative elastic-number and pair-lepton fluxes, folded Pauli Jacobian, exact FLRW energy debit, five-point derivatives, and a dual-solver private endpoint. The order-32/24 thermal moment reaches the independent F09B anchor to about `0.17%`, and the full 207-test release gate passes. | Calling the order-six endpoint momentum-converged; hiding its `~9.5%` thermal-moment deficit; accepting the order-eight/six dual-solver RED; treating coarse nodal distortion or `N_eff` as precision evidence; opening neutrino-neutrino, Type-I, QKE, or public runtime work. | F-10B1 is `VALIDATED / CLOSED_WITH_LIMITS` only as a coarse endpoint-consumed substrate. The next slice must close a momentum-order endpoint ladder and direct-support derivative sensitivity. F-11 stays parked; QKE/public production remain FORBIDDEN. |
| 2026-07-16 | Close F-10B2 with limits at the logit-coordinate, resolution-selected electron spectral endpoint and open only F-10C classical diagonal neutrino--neutrino work. | Direct occupations produced an honest grid-12 Rodas `nonfinite_rhs` in the far tail. The exact logit transform closes it without clipping or tolerance widening; analytic chain-rule Jacobians pass. A decomposed grid/event ladder rejects event four/three, selects grid 16 with event six/four within one percent of independent thermal moments, bounds explicit-`N` support-step sensitivity, reaches a dual-solver endpoint, and approaches a grid-20 BDF readout. | Calling grid-20 BDF an independent endpoint; hiding the skipped grid-20 Rodas leg; interpreting the roughly `0.117` resolved-tail distortion or `N_eff` as precision; adding a fitted table/RTA/readiness surface; porting the uncalibrated legacy neutrino--neutrino placeholder; opening Type-I/QKE/public runtime work. | F-10B2 is `VALIDATED / CLOSED_WITH_LIMITS` for numerical coordinate and selected resolution only. F-10C must derive primary-source channel normalization and exact conservative identities before endpoint consumption. F-11 stays parked; QKE/public production remain FORBIDDEN. |
| 2026-07-16 | Retain the nonnegative linear F10C0 deposition basis and resolve its radial-grid/infrared convergence before expanding the neutrino--neutrino catalogue. | The same-flavour CM component passes analytic normalization, conservation, equilibrium, entropy, Jacobian, high-order production-to-target checks, and the final 217/217 release/static gate, but the selected GL16 action remains about `7.54%` low. Global barycentric deposition is catastrophically ill-conditioned; local quadratic deposition improves one normalization probe but introduces negative weights and strong nodal stiffness. | Promoting the global barycentric basis; promoting the local quadratic basis from one weak-action probe; treating the green regression or dual-solver endpoint as resolution authority; completing row 3 or adding seven wholly absent rows before the active numerical blocker is closed. | F10C0 remains `IN_PROGRESS`: the same-flavour-identical component (row 1 plus a subset of row 3) is `VALIDATED` within its stated limits, while the selected-grid endpoint is only `IMPLEMENTED`. The next executable slice must move radial-grid/basis/infrared convergence against the tagged-target oracle; F-11 stays parked and QKE/public production remain FORBIDDEN. |
| 2026-07-16 | Select the fixed positive exponential N48 rule as the smallest tested F10C1 radial representation passing the declared state-basis envelope. | The shared map `y=-3 ln(1-t)` gives positive plain-`dy` weights without a user-imposed cutoff. Its direct N32/40/48/64 weak-action residuals decrease through `1.873525%/1.227393%/0.852441%/0.489704%`; two independent N128 auxiliary rules agree on exact references within `1.93e-8`, and five smooth nonthermal profiles give a worst state-basis residual `1.9771546%`, below the frozen `2.1%` ceiling. FD moments, electron thermal anchors, conservation, entropy, and focused Jacobian/FLRW checks pass. | Retaining GL16; accepting composite N32 after only its favourable direct ladder despite `~4.1%` auxiliary and `>1%` electron-anchor errors; accepting exponential N32 or N40; promoting negative-weight quadratic or unstable barycentric deposition; calling one direct scalar ladder sufficient; comparing the new endpoint with a grid-confounded GL16 electron-only `N_eff`. | Radial representation is `VALIDATED / CLOSED_WITH_LIMITS` only for the declared five-profile envelope. Catalogue row 1 is complete, row 3 is partial, and seven rows are wholly absent; an independent full spectral endpoint also remains absent. Precision distortion/`N_eff`, repeated-run authority, Type-I, QKE, and public production remain blocked or FORBIDDEN. The measured N48 cold wall, not another grid/readiness surface, determines the next executable slice. |
| 2026-07-16 | Reject and remove the F10C1 prepared same-flavour event-geometry cache after the matched N48 cold-endpoint gate. | The candidate reduced wall from `2181.07 s` to `1980.52 s`, only `9.195%` (`1.1013x`), below the policy's about-`10%` partial-blocker threshold. Max RSS increased from `51,020` to `52,692 KB` (`3.277%`). BDF was bitwise unchanged, but Rodas followed a different floating-point path while remaining inside the frozen solver envelope. | Retaining `112` Rust lines and a cache-only delegation test for a sub-threshold endpoint result; reporting the segment construction saving as endpoint promotion; widening the policy threshold after measurement. | The candidate and its cache-only test are `DEPRECATED` and absent from the current source. The negative timing remains recorded. F10C1 keeps the N48 radial representation and the independent electron values-only allocation removal; the measured cold wall remains the active performance blocker. |
| 2026-07-16 | Treat the retained electron values-only allocation cleanup as simplification, not F10C1 endpoint progress, and require a full-endpoint profile before another performance edit. | The current no-cache tree repeats both solver outputs bitwise but takes `2191.66 s` versus the `2181.07 s` baseline (`+0.486%`); RSS changes by only `-1.137%`. The cold wall is unchanged within run variation. | Claiming fewer allocations as a speedup without the same-case wall; opening another guessed cache or segment benchmark; reverting the arithmetic-neutral cleanup solely to chase timing noise. | Keep the three-line conditional allocation cleanup and sparse response structure for simplicity/N48 feasibility, assign them no blocker movement, and use the exact whole-endpoint `perf` route in the handoff to select the next dominant consumer. |
| 2026-07-16 | Stop the current programme at a completed and validated collision-coupled isotropic neutrino Boltzmann FLRW endpoint. | The owner explicitly redefined the target: finish F-10, then stop; Bianchi work will be separately reauthorized later. | Treating F-11 as an automatic continuation after F-10; beginning LRS/non-LRS geometry, shear, or anisotropic neutrino transport under the current goal. | F-11 Bianchi/Type-I is owner-paused. Until an explicit new instruction, all implementation and validation effort is limited to closing F-10 catalogue, performance, and independent full-spectral FLRW boundaries; after that, produce the handoff and stop. |
| 2026-07-16 | Retain the exact one-entry solver-local BDF Jacobian cache and the four-topology production collision aggregation; close the F-10 performance and catalogue gates. | The exact cache-bypassed/cache comparison reduces the dual-solver endpoint `2191.66 -> 968.38 s` (`55.815%`, `2.263x`) with bitwise-identical BDF/Rodas readouts and `+1.229%` RSS. After all nine rows were implemented, replacing seven production row contractions with four topology contractions reduces command wall `1520.92 -> 1184.77 s` (`22.102%`, `1.284x`) while an explicit seven-folded-channel test oracle, rowwise normalization, invariants, Jacobians, exact endpoint, and `230/230` release gate pass. | Approximate/cross-step/global Jacobian reuse; a runtime cache knob or telemetry surface; retaining seven duplicate production contractions; reporting profile or segment movement as endpoint progress; tuning `N_eff`. | F-10 catalogue and repeated-run performance are `VALIDATED` within their declared private N48 ceilings. The only scientific F-10 closure gate left is a structurally independent full-spectral FLRW comparison. Exact final solver-isolated wall remains unmeasured and must not be invented. |
| 2026-07-17 | Fail the first FortEPiaNO independent-comparison contract before output and require a new blind approval after executable resolution. | Blind review found mismatched frozen constants, stock fast-math, a pre-allocation diagonal-layout hazard, transformed raw state/collision/Hubble coordinates, an under-specified decreasing event algorithm, and non-executable norm/uncertainty definitions. A clean canonical `make tests`-only build passes the upstream suite, proving the pinned source baseline without producing an endpoint. | Running a fixed-`x` or nearest-output solve; using pristine source constants while claiming matched inputs; changing constants after seeing output; treating raw `delta`, collision arrays, or `hubble_factor` as physical observables; unblinding RABBIT before hashing the external bundle. | `C-F10-INDEPENDENT` remains `SPECIFIED` and the gate remains RED. A separate hash-locked comparison-input patch may align only the frozen constants and strict compile inputs, while collision formulae remain unchanged. Layout, physical mappings, event interpolation, and metrics must be executable and blind-approved before the first checkpoint/endpoint. This pre-output failure is not a physics disagreement. |
| 2026-07-18 | Stop the independent Galerkin-Petrov static M1 slice at the exact GL48 native mu-tau covariance failure. | The conservative event formulation passes every other recorded GL48 discriminator, but the prospectively coded native relative-L-infinity covariance is `4.666064056497196e-10`, 4.666 times the unchanged cap. Independent adjudication rules that `1/y^2` mass-inversion amplification is a realized conditioning risk of the selected method, not authority to change the norm after output. | Reweighting to the favourable modal residual; relaxing the cap; post-hoc mu-tau averaging; adding swapped event charts; retrying reductions; running GL64/Radau/endpoint; reopening Fort. | Preserve exact source/test/artifact hashes as implemented and validated-failure evidence. `G-F10-INDEPENDENT-FLRW` remains RED. Only materially new, prospectively registered owner-authorized numerical evidence may reopen a different method/norm; F-11 and QKE remain forbidden. |
| 2026-07-18 | Reject the current three-node maximum-relative-entropy route at design review before implementation. | The blind fixed-triple theorem is sound and the standalone binary64 rule passes local partition/moment/positivity checks, but the executable's GL-weight prior, nearest-exterior support selector, and exact-node one-hot branch are not derived as one continuous/covariant semidiscrete method. Support-switch/Jacobian behavior, the frozen 24-self/15-electron binary64 collision ledger, and the unchanged native mu-tau `1e-10` co-gate are absent. | Implementing the locally passing stencil; treating exact-node special casing as the same fixed-triple variational method; using its GL64 local audit as comparator authority; running collision, Radau, trajectory, endpoint, or RABBIT output. | Preserve the three exact blind result hashes and terminal `reject-design` adjudication. Production change is `+0/-0/net 0`; token use is `UNAVAILABLE`; blocker movement is `0.25`; verdict `FAILURE_MODE_LOCALIZED_REJECT_IMPLEMENTATION`. D-029 binds and `G-F10-INDEPENDENT-FLRW` remains RED. |

# BD629 — D-080C v2 Granular-Mutation Closeout

**Date:** 2026-09-02  
**Repository:** `cosmosapjw-quantum/RABBIT_v1.0.5_PRODUCTION`  
**Branch:** `research/d080c-tgamma-full-rhs-column-20260902`  
**Draft PR:** `#10`  
**D-080B predecessor:** `382711d2f7e6e59342390e3189096ebe3a4dc455`  
**Final v2 scientific workflow:** `33568699401` — **SUCCESS**  
**Workflow-tested head:** `800321b8b956e5ab7d28ea5036d127722f5fd46c`  
**Deterministic evidence commit:** `8ac3f39a63aaf723206114887cea6519297292b4`  
**Evidence tree:** `b662c538e73decce49a4bdd67712741cc23adce7`  
**Artifact ID:** `9824150571`  
**Artifact SHA-256:** `5bca87a5c464c830307ab14e74905a82b6e3a704c057a6727dea1cf9de7f98e9`

This closeout supersedes the publication identities, receipt schema, mutation
inventory, and mutation table in BD628.  The mathematical formulas and static
claim ceiling in BD627/BD628 remain unchanged.

## 1. Stable classification

```text
FULL_STATIC_TGAMMA_RHS_COLUMN
```

The admitted object remains the complete fixed-branch static input column

```text
partial F(c_e,c_mu,c_tau,T_gamma,t_elapsed;N) / partial T_gamma
```

of the frozen private comparator's original packed RHS at fixed `c`, `T_cm`,
and independent variable `N=log(a)`.

No production solver, tolerance, reaction catalogue, trajectory driver,
endpoint, or formal gate was modified.  No square-Jacobian, integrator,
performance, endpoint, `N_eff`, release, or publication claim is made.

## 2. Final v2 receipt identity

```text
schema:
rabbit.d080c.static_tgamma_rhs.v2

comparator Git blob:
de44feee0aa484abe26976c7dc34c579643005b5

receipt Git blob:
1ebb9a854f1db42d76ae36cfbe0926b016b67b7e
```

The residual contract is the maximum of separately dimensionless spectral,
photon-temperature, and elapsed-output residuals.  Rows with units MeV^-1,
1, and MeV^-2 are never mixed in one dimensional Euclidean norm.

## 3. Numerical results retained from the complete RHS audit

### Thermal split

```text
order                      8
y_max                      8
T_cm                       2.00 MeV
T_gamma                    2.05 MeV
best block residual        1.730599336173931e-07
base RHS reconstruction    0.0
component reconstruction   0.0
H_T/H                      0.5346295229727147 MeV^-1
same branch                true
```

### Manufactured weak-collision tail

```text
order                      8
y_max                      10
T_cm                       0.45 MeV
T_gamma                    0.50 MeV
best block residual        2.454956136786496e-08
same branch                true
status                     controlled static probe only
```

This weak-tail state is not retained late-trajectory evidence.

### Exact equilibrium

```text
dQ_nu/dT_gamma             +8.606919856943374e-20
dQ_em/dT_gamma             -8.606919856943374e-20
first-law residual         0.0
H_T/H                      0.5231017159993603 MeV^-1
elapsed-output tangent     -2.9682069952782854e20 MeV^-2
elapsed-input column norm  0.0
```

### Exact retained stiff state

The workflow recovered and checksum-verified

```text
source commit:
78f5b091bd8de99bd8d0b1eddd8a0b7978f5da2b

path:
.agent-harness/runs/run-20260805-f10-v3-campaign/v3a_r2/domain/state_1200.npz

SHA-256:
c0ad51adbd34d4fb8408f566c7bc573ca3d5bbd8c1d1f5d89f50df2e6b5d3380
```

and passed the order-60, `y_max=30` full packed-RHS discriminator on the
unchanged branch.

## 4. Granular v2 mutation audit

Residuals are measured against the best thermal original-packed-RHS centered
witness.

| Mutation | Block-scaled residual |
|---|---:|
| omit collision | `1.0220033536382722` |
| omit all Hubble feedback | `1.0` |
| omit spectral-row Hubble feedback | `0.021726827900600425` |
| omit photon-temperature-row Hubble feedback | `0.0116447366410172` |
| omit elapsed-output-row Hubble feedback | `1.0` |
| omit heat-capacity derivative | `0.625609561925678` |
| reverse `Q_em,T` sign | `0.9905287200456124` |
| swap photon-temperature and elapsed output rows | `1.0` |

The correct block residual is

```text
1.730599336173931e-07.
```

Every granular mutation is therefore resolved by at least five orders of
magnitude relative to the correct witness.  In particular, the v2 audit no
longer allows the very large elapsed-output row to kill an omnibus Hubble
mutation while leaving the spectral or photon-temperature Hubble terms
untested.

The elapsed-time **input** column is separately certified as an exact structural
zero by evaluating the original packed RHS at stored elapsed values `+-10^40`.

## 5. Figure audit closeout

The residual ladders are byte-unchanged because the underlying numerical arrays
and plotting contract did not change.  The two figures with long category
labels were changed from rotated vertical bars to horizontal bars:

```text
component_ratios.png
mutation_kills.png
```

This is a bounded legibility repair only; no numerical content or claim changed.
All seven evidence files were regenerated and checked for nonzero existence.
Publication-scale typography is not claimed.  These remain diagnostic figures,
not manuscript-ready figures.

## 6. TDD and orchestration record

The scientific result passed before publication repair:

```text
non-slow tests             4 passed, 1 deselected
retained slow test         1 passed, 4 deselected
receipt audit              PASS
exact retained-state SHA   PASS
artifact upload            PASS
```

Two publication-only failures were preserved:

1. run `33568152923`: the old evidence step attempted `git rebase` with
   regenerated tracked files in a dirty worktree;
2. run `33568352332`: the repaired publisher incorrectly required all seven
   evidence files to appear in the diff, although three deterministic plots
   were byte-identical.

The final publisher is fail-closed in two ways:

- the remote branch head must equal the workflow checkout head exactly;
- the nonempty changed-path set must be a subset of the seven admitted evidence
  files, while the v2 receipt, summary, component figure, and mutation figure
  must all be present in the change set.

No physics coefficient, tolerance, acceptance threshold, or scientific result
was altered during these publication repairs.

## 7. PHYS-MATH closeout

**No P0 finding remains.**

Passed:

- fixed-coordinate derivative definition;
- signs and energy-transfer convention;
- Hubble square-root derivative;
- photon-temperature numerator and denominator quotient;
- units and blockwise dimensional scaling;
- exact equilibrium restoring sign;
- differentiated first law;
- exact elapsed-input structural zero;
- massless `rho proportional T^4` EOS limit;
- same-branch finite-difference ladders.

Open:

- **P1:** ordinary differentiability is local to fixed support and
  matrix-correction branches;
- **P1:** no genuine retained late weak-collision state;
- **P2:** finite-temperature QED plasma corrections lie outside the frozen
  comparator;
- **P2:** retained order-60 coverage contains only one state and one thermal
  input direction.

## 8. PHYS-MATH-CODE closeout

**No P0 finding remains.**

Passed:

- exact predecessor object pins;
- original packed-RHS centered witnesses;
- exact base and component reconstruction;
- equilibrium, thermal-split, weak-tail, and retained-stiff tests;
- eight granular mutations;
- deterministic receipt and artifact publication;
- no production-path mutation.

Open:

- **P1:** full square static Jacobian not assembled;
- **P1:** no matrix/JVP identity or multi-direction certification;
- **P1:** no BDF/Newton telemetry or solver-stall claim;
- **P1:** private-helper drift remains controlled only by exact pins;
- **P2:** no independent second implementation;
- **P2:** dependency versions are pinned but wheel hashes are not.

## 9. Updated DAG

```text
D-079   spectral c-input RHS JVP              MERGED / ADMITTED
D-080A  moving T_gamma kinematics + EOS       MERGED / ADMITTED
D-080B  full static collision-action column   MERGED / ADMITTED
D-080C  full static original-RHS T column     VALIDATED / DRAFT PR #10
D-080D  full square static Jacobian            OPEN / NOT STARTED
D-081   stalled-prefix BDF/Jacobian probe      FORBIDDEN UNTIL D-080D
```

## 10. Next admissible node

D-080D must assemble

```text
J_static = [J_c | J_Tgamma | 0_elapsed]
```

with input and output order

```text
(c_e, c_mu, c_tau, T_gamma, t_elapsed).
```

Required exit gates:

1. exact shape `(3n+2,3n+2)` and machine-readable block/index manifest;
2. explicit matrix action equal to the existing static JVP;
3. basis-column reconstruction;
4. random and adversarial directional centered differences of the original
   packed RHS;
5. equilibrium, thermal split, exact retained stiff, and genuine retained late
   weak-collision states;
6. exact zero elapsed-input column;
7. blockwise dimension-aware residuals;
8. transpose, index-swap, missing-Hubble, missing-heat-capacity, sign, and
   nonzero-zero-column mutations;
9. sparsity and conditioning diagnostics without any solver call.

Only after D-080D closes may D-081 attach the operator to a separate
stalled-prefix BDF/Jacobian instrument.

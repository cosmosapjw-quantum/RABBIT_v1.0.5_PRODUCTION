# RABBIT - Frozen JAX Architecture and Migration Evidence

**Date**: 2026-07-12

**Status**: DEPRECATED as a promotion roadmap; retained as migration provenance

**SSOT**: `src/rabbit/config/backend_capabilities.py` and
`docs/TYPEI_AUGMENTED_NOQKE_UNIFIED_FUTURE_PLAN.md`

## Authority boundary

`backend="auto"` resolves to the SciPy Type-I reference. SciPy/BDF remains the
temporary number-of-record while Rust AOT is the SPECIFIED active
implementation and repeated-run design target.

All JAX forward surfaces are quarantined explicit compatibility paths. Their
historical `canonical` registry tier records bounded regression evidence; it
does not grant active runtime, future-development, publication, or production
authority. JAX is frozen to small local parity, AD, and Jacobian-oracle use.

No new JAX physics implementation, driver, endpoint path, solver-control
variant, or runtime-optimization PR may be opened in the augmented Type-I
no-QKE programme.

## Preserved evidence and algorithms

The migration does not discard what the JAX/Python line established. Rust work
may reuse, in Rust-optimized form:

- analytic characteristic maps and compact state layouts;
- finite-mass EOS and temperature-derivative algorithms;
- energy-variable conditioning and corrected collision scaling;
- nonautonomous Rosenbrock coefficients and fail-loud attempt semantics;
- analytic/block Jacobian and matrix-free JVP lessons;
- scoped SciPy/JAX parity fixtures and local AD checks.

This is semantic and algorithmic reuse, not a line-by-line translation or a
permanent NumPy/JAX/Rust triple implementation.

## Frozen explicit surfaces

- `backend="jax"`: historical linearized tier-1 live-weak regression surface.
- `backend="jax_characteristic"`: historical exact-characteristic tier-1
  regression surface.
- `backend="jax_characteristic_tier2"`: historical bounded three-temperature
  regression surface.
- `backend="jax_advanced"`: historical no-Teff weak-budget surface.

These names remain callable for compatibility and bounded falsification. They
are not quick-start recommendations and must not silently receive new physics.
The historical
`docs/JAX_MAIN_CANONICAL_PROMOTION_ROADMAP_2026-04-18.md` is provenance only;
its dispatch-flip ordering is superseded.

## Claim ceiling

- JAX algorithms and scoped parity probes: IMPLEMENTED.
- Previously executed bounded regression checks: VALIDATED only in their named
  numerical scope.
- JAX repeated-run/backend promotion: FORBIDDEN.
- Rust active implementation target: SPECIFIED until code executes.
- Rust number-of-record or public-production status: FORBIDDEN until the
  Rust endpoint authority and publication gates pass.

PUB-02C preserves its solver-primitive evidence but grants no endpoint or
runtime authority. G-01R remains open.
